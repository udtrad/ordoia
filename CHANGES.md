# Copy changes, flagged

BRIEF.md §8: the vault is the source of truth for this wording, and **nothing is
silently rewritten**. Every departure from the designer handover — commit `3b93f1b`,
byte-identical to the eleven files handed over — is listed here with where, source,
change and why. The format is the one `RATIONALE.md` already establishes.

`RATIONALE.md`'s own table records the ten changes the *design* pass made against the
vault copy. This table records what *engineering* changed against the design pass. The
two are cumulative and neither supersedes the other.

The rule §8 sets is applied throughout: typos, broken markup and factual mismatches are
**fixed and flagged**; tone, rhythm and word choice are **flagged and left**. Where a
row below says *flag and leave*, nothing was changed and the row exists so that the
next person does not have to rediscover it.

| # | Where | Source | Change | Why |
|---|---|---|---|---|
| 1 | Services, audit card | handover `services.html` L79 | "authorisation, refusal robustness, upgrade control, execution bounds" → the four full dimension names, rendered from `oal.json` | §8 names this exact sentence as an outstanding reconciliation and says **the rubric's names win**. The card's previous sentence already used full names, so the two halves of one paragraph disagreed. Both lists now render from `products.json` `auditCoverage` against the rubric's own names, so the reconciliation cannot be re-broken by an edit. Not a find-and-replace — the mechanism §6 exists for. |
| 2 | Services, under the page title | new copy | Added one `.note` line naming OAL v1.0, its publication date and its permanent address | Check 10 found that the services page states four depths, three readiness thresholds and eight dimension names against **no version identifier anywhere**. It is the page a buyer reads before spending £2,500, and a level means nothing without a version. This was not predicted by the brief; it was found by the suite. |
| 3 | Home, coverage × depth grid | handover `index.html` L128 vs `services.html` L58 | "the audit plus the top-up reaches exactly the same place as a baseline taken directly" → "audit plus top-up reaches…" (the services wording) | The same grid cell was hand-authored twice and the two copies had drifted. §6 requires one record; one had to win. The services wording was taken because the grid is that page's primary instrument and the cell is terse by design. Wording only — the claim is identical. |
| 4 | Rubric page, layout | handover `oal.html` L97, L398 | Added `.body { grid-column: 2 }` to `styles.css` | **Broken markup, fixed.** Two of the rubric page's `.sheet` blocks ship no `<aside class="rail">`, so `.body` auto-placed into the rail's column and the entire rubric — four levels, eight dimensions, depth grid, limits — rendered **152px wide** on desktop. Measured at 1280px, on the handover, before any build code. Every other page carries an empty rail and was unaffected. Stating the column is the fix that cannot be undone by omitting a rail again. |
| 5 | Everywhere, the measure | `RATIONALE.md` "Contrast" | New `--track` token `#7A827E` for `.measure__rule` and the minor `.tick`s; `--untravelled` keeps its existing job on table rules, field underlines and CTA dividers | `--untravelled` `#C3CAC6` is **1.37:1** on `--ground` and 1.51:1 on `--raised` — and on a *not assessed* scorecard row the minor ticks were the same colour as the rule they sat on, measuring **1.00:1**. The design pass's fix was real but moved only *text* off the token. `--track` is 3.25:1 on `--ground` and 3.57:1 on `--raised`, computed not quoted, and asserted at build time. |
| 6 | Everywhere, `.na` and `.mark--reader` | `BRIEF.md` §7 known latent defects | `background: var(--raised)` → `var(--surface)`, a context-driven property | §7 names this: a *not assessed* track placed on `--ground` showed a broken knockout. The reader's open mark had the same defect in reverse — filled with `--ground` on a `--raised` band, it read as a disc of the wrong grey rather than as an open mark. One property, set by the surface. |
| 7 | `styles.css` | `BRIEF.md` §7 | Deleted `.sheetpaper .na` | The brief lists it as "a rule with no matching markup". It is not: `<article class="paper sheetpaper">` exists in `scorecard.html`. It is a **redundant no-op duplicate** of `.na`, which is a different and smaller defect. Deleted as dead CSS; the record is corrected here. |
| 8 | `favicon.svg` | `BRIEF.md` §7 | The hardcoded `#E6EAE7` and `#171A1A` now come from `styles.css`, read at build time | §7 names the hardcoded value as a latent defect. Parameterising it into a second file would only move it, so the stylesheet stays the single source and the build parses the tokens out of it. Drift is now impossible rather than unlikely. |
| 9 | `RATIONALE.md` | measurement | "`ink` on `ground` 14.7:1" is **14.42:1** | Documentation only. Both figures pass AA by a wide margin, so nothing shipped was ever wrong — the number in the document was. Logged rather than silently corrected, because a rationale that quietly self-corrects is the thing this table exists to prevent. |
| 10 | Scorecard, the stamp and the *lowest level assessed* line | handover `scorecard.html` L68, L196 | Moved from template literals into `copy/scorecard.md` fragments | §8: copy is held in content files, not templates. These two lines are the most scrutinised wording on the artifact and were the last prose still assembled in markup. Text unchanged; `<b>` became `<strong>` as a consequence of markdown rendering, and `.stamp b` gained `.stamp strong` so it styles identically. |
| 11 | Every measure | `BRIEF.md` §7 | Added a visually hidden description list per scorecard measure, naming dimension, level, depth, basis, methodology version, working paper and the depth cap | §7 authorises added markup here explicitly: "a blind risk lead must be able to obtain the level, the depth, the version and the working-paper reference for every dimension". The visible stamp already carried all four as text, but ran them together with `·` separators and no labels. This names them. |
| 12 | Body type | `BRIEF.md` §4 | Self-hosted subset fonts replace three `<link>`s to `fonts.googleapis.com` / `fonts.gstatic.com`; `--body` gains a metric-matched `"Source Serif Fallback"` | §4's zero-layout-shift budget. The fallback is adjusted to the webfont rather than the webfont to the fallback: the other direction removes the same shift but renders the design's 17px body at an effective 16.38px permanently. Measured after the change — 0.613% width delta, 0px height delta across six real paragraphs. See `FONTS.md`. |
| 13 | Rubric page, every dimension | handover `oal.html` L156 and `styles.css` | `.selfcheck .eyebrow` gains `display: block` | **Broken markup, fixed.** The label was inline, so it ran straight into the question with no space: *"Self-checkIf you deleted the sentence…"*, on all eight dimensions. Its own `margin-bottom: 0.35rem` is inert on an inline element, which is the tell that a block was always intended. Measured on the handover before any build code. |
| 14 | Every data table | `BRIEF.md` §7 | Tables wrapped in a focusable `.scroller` with `overflow-x: auto` | §7 asks for 320px and 200% zoom to be tested. At 320px the coverage × depth grid pushed the whole page 58–61px sideways on Home and Services, and the version index 7px on the changelog — so every paragraph on those pages needed horizontal scrolling to finish. WCAG 1.4.10 exempts data tables from reflow because they need two dimensions; it does not exempt the page. The table scrolls in its own box now and the page does not. Found by check 13. |
| 15 | `RATIONALE.md` | measurement | "`untravelled` is used for rules and fills only" now records that the measure's rule and ticks are drawn in the new `--track` | The document described a token that no longer draws the instrument. Corrected in place, with the change logged here rather than made quietly — the same standard the table itself sets. |
| 16 | Everywhere the date appears | practice decision, 2026-08-08 | Publication date `2026-08-07` → **`2026-09-19`** | §12.2 flagged this as not ours to decide, and it has now been decided. One edit in `src/_data/site.json`; it propagated to seven built files with no other change, which is what §12.2 asked the build to make possible. It is now a permanent address and a version stamp on every scorecard issued under OAL v1.0. |
| 17 | Home, failure block | §12.3, released by the practice | The two missing failure-mode bullets — instructions arriving as data, unbounded execution — **written** | §12.3 said leave room and do not write them. That stop has been lifted deliberately. Until now the block's six named failures did not motivate dimensions 4 and 8, so the page argued for a rubric wider than the failures it described. New copy, in the established voice: a bold noun phrase naming the failure, then one concrete sentence. Not drawn from the vault — flagged here as authored. |
| 18 | Print stylesheet | check 14 | `@page { margin: 16mm 14mm }` → `@page { size: A4; margin: 16mm 14mm }` | **Broken, fixed.** The rule declared a margin but no page size, so Chromium fell back to its locale default and the generated scorecard came out **US Letter (612×792pt)** — a UK assurance practice's travelling artifact on the wrong paper. Caught by reading the MediaBox out of the generated PDF rather than trusting the flag that asked for A4. Fixed in the stylesheet rather than the PDF tool, because File → Print from the page is how most readers will make a copy. |
| 19 | Scorecard, About this form | §6 | The pointer to the other two formats moved into `copy/scorecard.md` | It was written into the template when the PDF link was added. Check 12 caught it within one build, which is the check working exactly as intended on its author. |
| 20 | Everywhere the address appears | practice decision, 2026-08-08 | Domain `ordoia.co.uk` → **`ordoia.com`**, and `hello@ordoia.co.uk` → `hello@ordoia.com` | The registered domain is `ordoia.com`; the site had been built throughout against `ordoia.co.uk`. Like row 16 this was one edit in `src/_data/site.json`, which is what that file exists for. It is more permanent than the publication date: `ordoia.com/oal/v1.0` goes on the face of every scorecard ever issued under OAL v1.0, and after the first one is issued it cannot be changed, only redirected. Corrected before publication, which is the only time it is free. The old domain is recorded as `formerDomains` — see row 21. |
| 21 | `src/_data/site.json` | check integrity | New `formerDomains: ["ordoia.co.uk"]` | Not copy, and nothing in the build reads it — the checks do. The designer handover at `3b93f1b` is frozen byte-identical and still says `ordoia.co.uk`, so a matcher that knew only the current domain found nothing there and reported baseline B red for the wrong reason. Recording the domain's history is also the honest form of the change in row 20: the practice published against one address and now publishes against another, and that is a fact about the practice rather than a detail of the build. |
| 22 | `src/_headers`, `/*` block | Cloudflare Pages documentation | `Cache-Control: public, max-age=600, must-revalidate` **removed** from `/*`; a `! Access-Control-Allow-Origin` detach line added | **Broken, fixed, and it would only have shown up in production.** Cloudflare documents that a request matching several `_headers` blocks inherits *all* of their headers, and that a header declared twice has its values **joined with a comma**. Every asset under `/oal/v1.0/` matched both `/*` and `/oal/v1.0/*`, so the frozen version directory would have been served `public, max-age=600, must-revalidate, public, max-age=31536000, immutable` — two max-ages on the one directory that can never be corrected after publication. Check 15 asserts that value matches `/immutable/`, and the joined string does, so the wire-level check would have stayed green over it. Current paths now take the host default, `public, max-age=0, must-revalidate`, which is *shorter* than what was declared, so §5's "short on current paths" holds a fortiori. The ACAO line is pre-emptive: check 15 has always told the operator to remove that header exactly this way, and until now the line it named did not exist and the parser could not have seen it. |
| 23 | `tools/build-fonts.sh` | measurement | `export SOURCE_DATE_EPOCH=1786147200` | **A published claim that was false, made true.** The script's header says a clean checkout should reproduce the committed `.woff2` files byte for byte in six years. It did not: `varLib.instancer` writes the wall-clock time into the head table, so two runs seconds apart produced `head.modified=3869118376` and `3869118380` and differed by twenty bytes downstream — and neither matched what was committed. `ibm-plex-mono` was byte-stable throughout and is the only face that skips the instancer, which is what located the cause. Verified by running the whole script twice and diffing the hashes. The committed subsets and the sha256 block in `FONTS.md` changed as a consequence, for all three instanced faces. |
| 24 | `src/fonts/source-serif-4-italic-subset.woff2` | §4's page-weight budget | Declared character set narrowed from 110 codepoints to **34**; 23,428 B → **10,852 B** | §4's "no page over 150 KB" was recorded in `CHECKS.md` as a miss and left, on the argument that narrowing the italic traded a stated budget against a fragile asset. It is taken now because the fragility became executable: **check 18** fails the build naming any character set in italic that the subset does not declare. The rubric pages go from 151.5 KiB to **139.1 KiB**. It had to land before `/oal/v1.0` first publishes — §5 freezes that directory and `_headers` caches it `immutable`, so after the first production deploy the font could never have been changed and the overage would have been permanent. 34 rather than the 31 that render: completing lowercase costs 1,068 B, completing uppercase would cost four times that. |
| 25 | `FONTS.md`, `tools/build-fonts.sh` | measurement | *"`.buyerq` … appears five times"* → **eight**; *"four inline `<em>` runs"* → **six**; *"all of them resolve to `--body`, i.e. Source Serif 4"* → **withdrawn** | Documentation only, and the third one mattered. Measured with Playwright across all nine pages: the `<em>` runs compute to `Archivo, system-ui, …` — a roman-only family — so the browser synthesises an oblique and **fetches no italic file at all**. Only the eight `.buyerq` pull-quotes ever load the italic woff2, which is what makes its character set knowable and row 24 safe. The paired figures *"61,436 B against 23,412 B"* were dropped rather than corrected: 23,412 was never the size of any committed file, and neither number is re-derivable after rows 23 and 24. Quoting a figure nobody can check is the OAL 1 failure this practice sells against. |
| 26 | `CHECKS.md`, budget section | measurement | The recorded 118–121 KB and 151.3 KB figures replaced with check 17's output, and a **draft plan's claim that six pages pull the italic withdrawn** | The recorded figures were substantially right; the correction proposed against them was not. A draft of the launch-blocker plan asserted six pages pull the italic and that `/scorecard/` and `/services/` were really ~144 KiB. Measured per page against the real request set: false. Six pages fetch the *mono*, and six pages *render* italic text, and those are two different sets of six, neither of which is "pulls the italic file". Logged because the doubted claim was the correct one, and a table that survives being doubted should say so. |
| 27 | `CHANGES.md`, "Closed since" | `git remote -v` | *"something that has now been settled: the repository is public"* → **withdrawn**, and the dependency stated | **A false claim in the provenance section of a practice that sells provenance.** There is no git remote at all — the repository has never been pushed anywhere — so a reader following the reasoning to its premise finds nothing there. The decision it supports is unaffected and stands; what was wrong was recording an unmet precondition under a heading that means "settled". This is the OAL 1 failure in miniature and inside our own instrument, which is why it is a row rather than a quiet edit. |
| 28 | `src/_data/site.json`, `$comment` | `CHANGES.md` row 16 | *"It is NOT settled — it needs confirming before launch"* → the date is recorded as decided | The publication date was decided as `2026-09-19` on 2026-08-08 and row 16 records it. The comment beside the value went on saying the opposite, so the one file that exists to be the single source of truth was internally inconsistent — and it is the file every printed scorecard address is built from. Documentation only; the value was always right. |
| 29 | `src/styles.css`, italic `@font-face` | measurement | *"the five `.buyerq` pull-quotes"* → **eight** | Row 25 corrected this same count in `FONTS.md` and `tools/build-fonts.sh` and missed the third copy. Counted in the built page: eight. Logged rather than silently fixed because the interesting fact is not the number, it is that a correction applied to two of three places is how a wrong figure survives being corrected. |
| 30 | `tests/checks/07-contrast.test.js`, header | `RATIONALE.md:82` | *"RATIONALE.md says 14.7 — see below"* and the paragraph explaining the discrepancy → updated to record that it was fixed | Row 9 flagged `RATIONALE.md`'s 14.7:1 as really 14.42:1, and `RATIONALE.md` was subsequently corrected. The check's header still described the disagreement in the present tense, so it told a reader to go and find an error that is no longer there. A comment that sends someone after a fixed defect costs the same as one that hides a real one. |
| 31 | `.github/workflows/`, `tools/`, checks 19 and 20 | launch-blocker plan, stage 4 | Domain single-sourced out of both workflows; every action pinned to a commit SHA and wrangler to `4.120.0`; `--branch` named on every `pages deploy`; two-stage deploy with a preview gate; rollback via the Pages REST API with a narrow probe | **The deploy path was the only part of this repository with no check behind it**, and it is the part that touches production. Three defects, each a shape the suite has been bitten by before: the domain written out by hand in two places (lesson 8, again), every action floating on a major tag in a repo that pins Eleventy to a patch version, and a `pages deploy` with no `--branch` — which, measured against wrangler 4.120.0, would have landed every "production" deploy as a preview while the deploy step went green. Checks 19 and 20 make all of it executable. |

| 32 | `tools/cf-api.mjs`, `tools/zone-setup.mjs`, `tools/dns-plan.json`, check 22, `canary.yml`, `DEPLOY.md` | launch-blocker plan, stage 6 | The Cloudflare zone's posture became a table that is both applied and asserted, and the weekly canary now reads it | **`DEPLOY.md`'s zone hardening was a manual checklist, and it was missing an item.** Speed Brain is enabled by default on the Free plan and adds a `Speculation-Rules` response header telling browsers to prefetch; it leaves the body byte-identical, so **check 15 is structurally incapable of seeing it** — byte-equality passes and the header assertions do not look for it. Every other mutating default this page names (Email Obfuscation, Rocket Loader, Bot Fight Mode) leaves `/cdn-cgi/` in the HTML and is therefore already caught. This one was the exception, and it was undisclosed prefetching on a site whose argument is that the edge does not touch what it published. `evaluateZone()` treats a target setting **missing from the API response** as a failure rather than a pass, because the naive form — "of the settings returned, is each one what we wanted?" — reports green against a renamed setting, which is lesson 8 with the denominator moved one layer down. Proven by mutation: replacing that branch with `continue` turns the controls red naming the case. |
| 33 | `tools/pages-api.mjs` | refactor | Its private `credentials()` and `call()` moved into `tools/cf-api.mjs` | Stage 6 added four more callers of the Cloudflare API, and a second copy of "read the token, build the URL, decide what a refusal means" is a second place for those decisions to drift — the defect `tests/lib/posture.js` was created to end, where check 14 and check 15 each had their own header evaluator and check 14's was wrong. Exports are unchanged and check 20 was re-run against the refactor before anything else was built on it. The new client takes the permission each call needs *from the call site*, in the dashboard's own wording, rather than mapping Cloudflare's numeric error codes to remedies — a hardcoded table of someone else's error codes fails silently when it drifts, by printing a confident wrong instruction. |

| 34 | `tools/pages-api.mjs`, check 20 | the rollback drill, run against the real API | `selectRollbackTarget()` (newest successful production deployment) → `selectServingDeployment()` (`project.canonical_deployment`) | **The recovery path recovered onto the wrong bytes, and the old code's own comment described the bug.** Deploy A, deploy B, roll back to A: Cloudflare keeps B in the production listing, newest-first and still `success`, and records A separately as `canonical_deployment`. The listing scan therefore answered **B** — the deployment just rolled away from, quite possibly *because it was bad*. `deploy.yml` captures this value before promoting, so after any rollback the next failed deploy would have "recovered" onto exactly the bytes rejected last time, reported success, and passed the probe. The comment above the function read *"Rolling back to the wrong deployment is worse than not rolling back at all: it looks like a recovery."* It was describing itself. Check 20's fixtures are now built from observed responses rather than documented ones, and carry this case by name. |
| 35 | `.github/workflows/deploy.yml`, `tools/zone-setup.mjs` | the rollback drill | A `purge-cache` step between the rollback and the probe | **A rollback did not change what a reader receives.** Measured: after rolling back, the edge went on serving the pre-rollback bytes and said so — `cf-cache-status: HIT`. A cache-busting query string got the rolled-back page; a plain request did not, and neither did a `Cache-Control: no-cache` request header. Without the purge the rollback is an operation that changes nothing anyone can see. **The probe cannot detect it**, and that is the interesting part: the probe asks whether *a* healthy page is served, not *which* one, and a stale page is a healthy page. Zone-scoped, so it protects the custom domain and not `*.pages.dev` — one more reason the preview stage is a gate rather than a place to recover. |
| 36 | `tools/wait-for-origin.mjs`, both check-15 steps, check 19 | the rollback drill | Deploys now wait for the host to serve *this build* before it is checked | **A Pages hostname lags its own deployment**, measured at 404 on the alias at t+3s and 200 by t+8s while the deployment's own `<hash>.pages.dev` URL had it immediately. Both check-15 steps ran inside that window. On the preview stage that is fail-safe — it refuses to promote. **On the production stage the workflow answers a failed check by rolling back**, so a few seconds of edge lag would have undone a good deployment and explained it in the job summary as bad bytes. The wait compares bytes rather than waiting for a 200, because the page served *before* the deploy is a 200 too, and it distinguishes "never reachable" from "reachable, serving different bytes" — the second is a real failure and the message says so rather than blaming the race. Check 19 fails the suite if a deploy ever loses its wait; proven by removing one. |
| 37 | `tools/dns-plan.json`, `DEPLOY.md` | measurement | The apex CNAME is created by this repository, not by Cloudflare | Cloudflare's custom-domains page says that with the nameservers pointed at it, Cloudflare *"will proceed by creating a CNAME record for you"*. That is the dashboard flow. `POST /pages/projects/{p}/domains` accepted the domain, returned `status: pending`, and stayed there reporting `"CNAME record not set"` while the apex had no address record and did not resolve for anyone. **The fourth time this project has been misled by Cloudflare documentation that is true in one context and false in the one that matters** — after the `_headers` comma-join, the `pages deploy` branch inference, and row 34. The pattern is worth more than any single instance: their documentation describes the dashboard, and this repository drives the API. The documented *order* still holds and is followed: associate the domain first, then add the record. |
| 38 | `tools/zone-setup.mjs`, check 22 | measurement against the live zone | `replace_insecure_js` and `server_side_exclude` added to the posture table, both measured **on** | Neither was in `DEPLOY.md`, because both were found by reading the zone rather than by reading about it. The first rewrites HTML to substitute Cloudflare-hosted JavaScript libraries; the second strips content between markers for visitors Cloudflare judges suspicious, so one URL can serve different documents to different readers. Both are no-ops on this site — there is no JavaScript and there are no markers — and both are off now, because a posture that holds only because of what the site happens not to contain is not a posture. Also recorded: `speed_brain` is absent from the settings *listing* and readable only per-id, and Cloudflare documents it as on-by-default while this zone reported it off. |

| 39 | `src/styles.css`, the measure block; new check 23 | the reader, looking at the page | `.span` moved from `position: absolute` into normal flow; the rule's line redrawn as `::before`; `.measure--q`'s `padding-bottom` 6.5rem → 0.5rem | **The instrument was illegible on three of its five instances, on every desktop width, from the day the site launched.** The untravelled span hung out of flow from a 1px rule and printed straight through the stamp paragraph below it — measured at **36.5–55.4px of overlap** on `/`, `/oal/` and `/oal/v1.0/`. `.measure--q` reserved the span's height at the *figure's* bottom edge, past the stamp rather than between them, so the reservation never separated the two things that collided. It arrived in the design handover (root `styles.css:225`, `:250`) and survived a design pass, a brand review, twenty-two checks and a launch. **Check 13 tested this element and passed**: it asks whether the question is present, visible, non-empty and non-collapsed, and every answer was yes. Text can be all four of those and still be unreadable, because something else is printed on top of it. The fix is content-driven rather than a larger constant — the span reserves its own height — so a longer question at a future width cannot reopen it. Markup is untouched, which is what kept `/oal/v1.0/index.html` byte-identical through the re-freeze in row 40. |
| 40 | `eleventy.config.js`, `tools/freeze-version.mjs`, new `versions/v1.0/`; `versions/v1.0.json` re-taken | row 39, blocked | The version snapshot is served from bytes stored at publication instead of re-copied from `src/` on every build | **Two comments described a property the build did not implement, and one of them sat directly above the code that lacked it.** `eleventy.config.js` said the frozen version is self-contained so that *"a colour change in 2028 would silently alter a methodology document"* — and then copied the living `src/styles.css` into `/oal/v1.0/`. `src/_headers` repeated the claim. The snapshot was self-contained in its *paths*, never in its *bytes*. Nothing had noticed because check 21 hides this in the most misleading way available: it holds the built snapshot to a manifest, so the coupling never appeared as a frozen page changing — it appeared as **the living stylesheet being un-editable**, one check going red on a file the edit was not about. Row 39's fix was unshippable behind it. A version with stored bytes is now served from them and `src/` cannot reach it; a version with none is being published for the first time, where live source *is* its published content, so the fallback is correct rather than lenient. Proved by drill: append a byte to `src/styles.css`, rebuild, check 21 stays **green** while the live stylesheet's hash moves and the snapshot's does not. **v1.0 was then re-frozen** — `styles.css` `a14dd87f` → `81e8c155`, `index.html` unchanged at `0289c300`, confirming the fix was genuinely CSS-only. Re-freezing a published version is the thing the manifest's own header forbids, and it is taken deliberately here and only here: v1.0 was two days old, unannounced, cited on no scorecard, and its stylesheet was still a cache MISS at the edge. This is the race row 24 won for the italic subset and row 39's defect lost by one day. **Still generated rather than pinned: `index.html` and `favicon.svg`** — a layout change will turn check 21 red and force this decision again, deliberately. |
| 41 | New `tools/monitors.json`, `tools/monitor-setup.mjs`, check 24; `DEPLOY.md` corrected | measurement against the live site | The external monitor became a plan the repository holds the provider to, and two of `DEPLOY.md`'s four assertions were wrong | §9's liveness cannot live in `canary.yml` — GitHub disables scheduled workflows after 60 days of quiet and this site is quiet by design — so the monitor runs at Better Stack. Its *configuration* does not have to leave, and four monitors described only in someone's memory of a web form is the failure this practice sells against. Two errors found by measuring rather than by re-reading: `DEPLOY.md`'s table asserted `mailto:hello@ordoia.com` on `/oal/v1.0`, which contains that string **zero** times, so the monitor would have alerted on its first run; and it printed the address **without its trailing slash**, which returns 301, so a keyword check that does not follow redirects reads a redirect body and finds none of the strings it is looking for. Check 24 carries check 22's decision one layer out: **a planned monitor missing from the API response is a failure, not a pass**, because deletion is how monitoring actually dies and the per-returned-monitor evaluator reports green against an empty account. `ssl_expiration` and `domain_expiration` come free on the same object, which turns §5's domain-lapse worry from a paragraph nobody has written into an alert. |

| 42 | `CHECKS.md`, *Not yet built* | `CHECKS.md`, *The rollback drill*, 300 lines earlier | *"the Cloudflare API calls behind them have never run"* → withdrawn; the drill is recorded as run on 2026-08-10 | **The document contradicted itself, and both halves read as authoritative.** The rollback drill was performed on 2026-08-10 against the real `ordoia` project, and `CHECKS.md` has printed the observed API responses ever since — while its own *Not yet built* list went on saying those calls had never happened. Found while adding checks 23 and 24, not by looking for it. This is rows 28 and 30 a third time: a correction landed in one place and not in the other, and the interesting fact is not the sentence, it is that the suite has no check that reads its own documentation for internal consistency, and probably cannot have one. |

| 43 | `DEPLOY.md` re-freeze recipe, `tools/freeze-version.mjs` refusal message, new check 21 test | pre-landing review, same session | *"`rm versions/v1.0.json`"* → **`rm -r versions/v1.0.json versions/v1.0/`**, and the refusal message corrected to name both | **The procedure written in row 40 was defeated by the mechanism row 40 introduced, in the same commit.** Once `versions/v1.0/` exists the build serves the snapshot from it and `src/` cannot reach it — which is the entire point — so a re-freeze that deletes only the manifest rebuilds the *stored* stylesheet, hashes it, and records the identical bytes. **The command reports success and changes nothing.** The drill that proved the decoupling (append a byte to `src/styles.css`, watch the snapshot hold at `81e8c155`) is the same fact read from the other side, and it was in this file before the recipe was written. Also: check 21 could not have caught a regression pointing the build back at `src/`, because the stored stylesheet and `src/styles.css` are byte-identical today, so **both arms of the branch produce the same manifest and the same green** — the property was proved once by hand and then held by nothing, which is §13 item 6's definition of documentation. A fourth check-21 test now compares the built snapshot to the stored bytes directly. Mutation-tested: forcing the build back to `src/` turns it red naming both hashes. |
| 44 | `tools/monitor-setup.mjs`, `COMPARED` and the PATCH body | pre-landing review | `url`, `monitor_type`, `required_keyword` **removed**; `sms`, `call`, `regions`, `request_timeout` **added**; drift now PATCHes only the changed fields | Three of the listed fields were dead: they are the identity triple, so any monitor `diff()` matches already agrees on all three and the comparison could never fire. Listing them read as coverage. Meanwhile `monitors.json` spends a paragraph arguing that SMS and phone alerting are off — and nothing enforced it, so **check 24 passed with them switched on at the provider**, which is the state the plan file rejects. An opinion written in a comment and not in the array is an opinion nothing enforces. The PATCH sent the whole planned object, overwriting `pronounceable_name` — deliberately excluded from both `identity()` and `COMPARED` because names are prose — and contradicting `COMPARED`'s own promise that unlisted fields are left alone. `sameValue()` also treats a field the API omits as drift rather than agreement: `String(undefined)` compares equal to nothing, and absence is how a setting you believe you configured turns out never to have been sent. |

| 45 | `.github/workflows/deploy.yml` | measurement against the live edge, before landing row 39 | A `purge-cache` step on the **success** path, not only the failure path | **The recovery path was hardened against stale bytes and the success path was left open, which is the wrong way round: a rollback is rare and a deploy is every time.** `_headers` caches `/oal/v1.0/*` `immutable` for a year, so a deploy that changes a byte under that prefix is a deploy no returning reader receives. Measured on 2026-08-11, before merging row 39: `/oal/v1.0/styles.css` returned `cf-cache-status: HIT` with `age: 7854`, holding exactly the bytes the deploy was about to replace — so the legibility fix would have landed, passed every check, reported success, and reached nobody who had already loaded the page. This is the 2026-08-09 drill's finding (*"a rollback does not change what a reader receives"*) one path over, and it was found the same way: by asking the edge rather than by reading the workflow. Deliberately unconditional — a step that fires only when someone remembered to mark a deploy as touching a frozen version is a step that fires when it is least needed. Check 15 proves the host was *given* the right bytes; this is what makes a reader receive them. |

| 46 | `src/_data/copy/services.md` `retainer.terms`; new `retainer.priceCap` token in `eleventy.config.js` | measurement, while mapping the price surface | A hand-typed `From £3,000/month` → `{retainer.priceCap}` | **One of the five prices was not wired to the data, and the token that should have carried it was dead.** `{retainer.price}` was defined in `buildTokens()` and referenced by nothing, while card 3's header typed the amount out in full — so changing the retainer's price in `products.json` moved the grid cell and left the card header saying the old number, on the same page. Found by grepping every `£` in the tree rather than by reading, and it is the argument for the filter in one line: twelve of the thirteen price sites were already interpolated, the thirteenth had drifted out, and nothing could see it. The capitalised variant exists because card 3's header opens with the price and the grid cell does not — the same shape as `partyWord`/`partyWordCap`, and for the same reason: a capital is a rendering concern, so putting it in the data would leak it into the mid-sentence grid cell. Output is byte-identical across all 32 non-PDF files. |
| 47 | `tools/build-pdf.mjs`, recorded not yet fixed | measurement, two consecutive builds | *(none — this row records a defect, see below)* | **The scorecard PDF is not byte-reproducible, and this was found while proving something else.** Two builds seconds apart with **no source change at all** produced `5dd7a819…` and `f9f9309f…`; every one of the other 32 built files was byte-identical across the same pair. This is row 23 one file over — `varLib.instancer` wrote wall-clock time into the font tables, and Chromium writes a creation date into the PDF — and it makes `BRIEF.md` §"Deploys are reproducible from a clean checkout" and `DEPLOY.md`'s "makes the build reproducible" false for one artifact. It does **not** touch the freeze: `/oal/v1.0/` contains no PDF, so check 21 is unaffected, and check 14 reads the MediaBox rather than a hash. It is recorded rather than fixed because it is outside this session's scope, and because the fix has a choice in it (pin a date, or strip the trailer) that should be taken deliberately. **Any byte-identity claim in this repo must exclude the PDF, and now says so.** |

| 48 | `eleventy.config.js` `renderPrice()` | draft 5 §4.1 | Every rendered price gains `+ VAT`, joined with non-breaking spaces, with no opt-out parameter | The suffix is unconditional because every published rate on this site excludes VAT, so a call site *able* to render one without the suffix is a call site able to be wrong — and an option defaulting the right way is still an option somebody can pass. The order is the reason this is a function rather than a convention: the period binds to the amount and VAT comes last, so the retainer renders `£3,000/month + VAT` and can no longer render `£3,000 + VAT/month`. **§4.1's `<span class="price">` with `white-space: nowrap` was tried and does not work, measured rather than assumed.** A price reaches the page by two routes and markup survives neither: copy fragments run through markdown-it with `html: false`, which prints `&lt;span class=&quot;price…`, and the three card headers use no `md` filter at all, so Nunjucks' own auto-escaping does the same. A `&nbsp;` *entity* is escaped to `&amp;nbsp;` on that second route and prints literally. A literal U+00A0 survives both intact, and non-breaking spaces are the mechanism for "must never break after the `+`" in the first place — so the guarantee is kept, the markup is dropped, and no second rendering path is added for copy, which would have defeated the point of having one filter. |
| 49 | Nothing changed — this row records a measurement | draft 5 §4.2, which asks for this to be tested rather than trusted | The claim *"check 12 will not churn, because skeletons leave the value free"* is **confirmed**, and the model behind it is **wrong** | Check 12 is green with the suffix on, and all four price-bearing sentences it traces still match: the skeleton wildcard is `[a-z0-9£% ]*?`, which is exactly the alphabet `normalise()` leaves behind, so `£2,500` growing into `£2,500 + VAT` grows *inside* the gap — the `+` is stripped, `VAT` folds to `vat`, and both stay in the class. But §4.2 says "confirm all thirteen skeletons still match", and there are not thirteen. **Measured: 14 price-bearing rendered units, of which 4 enter check 12's population and 10 are silently skipped.** `units()` splits on `·` *before* sentence-splitting and `MIN_WORDS` is 8, so every card header and every grid cell — `£2,500 + VAT fixed`, `from £3,000/month + VAT` — is a 3-to-5 word field that is `continue`d before it is even counted. The consequence is worth more than the confirmation: **check 12 protects 4 of 14 price strings and structurally cannot see the other 10**, so typing `+ VAT` into copy would not have rewritten thirteen skeletons either. Check 26 is the only thing guarding those ten, which makes it load-bearing rather than belt-and-braces. |

| 50 | `tools/freeze-version.mjs` `PINNED_ASSETS`; new `versions/v1.0/index.html` and `versions/v1.0/favicon.svg` | Gate 0, before any code was written | `index.html` and `favicon.svg` join the pinned assets, so the snapshot is served from stored bytes rather than regenerated from `src/` | **Row 40 deferred this decision and predicted precisely when it would come back: *"a layout change will turn check 21 red and force this decision again, deliberately."* It came back on schedule.** The footer is not a partial — it is inline in the one layout, and `/oal/v1.0/index.html` is one of nine built pages that render it, so a footer carrying a VAT number moves the frozen snapshot's bytes. Gate 0 established this by grep before a line was written: prices reach that page **zero** times (the only hits are the word "price" in prose), and the footer reaches it every time. **Pinning is not re-freezing, which is the whole reason this was the option chosen.** The stored bytes *are* the published bytes, so `versions/v1.0.json` is untouched, `index.html` stays `0289c300`, and check 21 is green before and after — where a re-freeze would have re-recorded different bytes against a published address, spending for a second time in two days an exception `DEPLOY.md` records as "not a precedent", on a change that is not about the rubric at all. Publishing v1.1 was rejected for asserting a methodology change that did not happen: the changelog's own test is that a change is clarifying only if an unchanged system could not be scored differently, and a footer cannot move any score. **Proved by drill rather than argued**, the same shape as row 40's: replace the footer text, rebuild — live pages move to `66b1f3cc` and `7405dde7`, the frozen page holds at `0289c300`, the drill string appears in the live page once and in the frozen page **zero** times, check 21 stays 4/4. Cost, stated rather than discovered later: the frozen page's footer and nav now hold their published state, so a page added in 2028 will not appear in v1.0's footer list — which is what "frozen" means. `favicon.svg` is included because it is generated from `--ground`/`--ink` read out of the *live* `src/styles.css`, so a colour token could reach the snapshot even though `styles.css` itself was pinned; that coupling was undocumented and is now closed. |

| 51 | New fifth test in `tests/checks/21-version-freeze.test.js`; header corrected | measurement, immediately after row 50 | While a version's status is `Current`, its live page and its snapshot must state the same rubric | **Pinning `index.html` closed one coupling and opened a quieter one, and the quieter one is worse.** Before row 50, `/oal/v1.0/index.html` was regenerated from live `oal.md` every build, so editing the rubric's copy moved the snapshot and turned check 21 red — loudly, and that noise *was* the guard. After row 50 the snapshot is served from stored bytes, so the same edit changes `/oal/`, leaves `/oal/v1.0/` untouched, and **check 21 stays green while one version number names two different documents**. A scorecard citing v1.0 would not say which of the two addresses its reader should have used. Demonstrated rather than argued: adding a clause to `dimensions.body` leaves the first four check-21 tests **green** and turns only the new one red, naming the divergence at word 554 with both readings printed. The comparison is over prose, not bytes, because the two pages differ legitimately in chrome — `assetBase` rewrites asset URLs, the canonical link differs, the masthead marks a different nav item — and measured today their prose is identical at 4,836 words each. The assertion is scoped to `Current` so that it lapses exactly when v1.0 is superseded, which is the moment the two are *supposed* to diverge. |
| 52 | `tests/lib/harness.js` `SKIP_DIRS` | the handover baseline moving for no nameable reason | `versions` added to the directories `htmlFiles()` does not scan | **The handover's failure count moved 9 → 12 and the three new failures were an artifact of the test harness, not a defect.** `versions/` had contained only passthrough assets until row 50 put an `index.html` in it; the handover target is the repo root, so from that commit the handover run began scanning a fully-built rubric page and evaluating it against the *handover's* stylesheet. Checks 2, 4 and 9 went red on a comparison between two unrelated things. Those bytes are frozen and could never be fixed in response, so the reds would have been permanent — a baseline nobody could interpret, which `CHECKS.md` names as the specific thing that makes the handover number worth watching. `versions/` is skipped for the same reason `_site` is: it holds output, not source. It is not left unguarded — check 21 holds every byte of it to the manifest, which is a stronger statement than any of these checks makes. **Found only because the delta was predicted first and the measurement disagreed.** |

| 53 | `src/_data/copy/home.md` `hero.sub` | draft 5 §6.1 | The word `UK` deleted from the sector list | The sentence read "for UK financial services, private equity and professional services firms". The practice is UK-based and the footer says so as its own field; putting it inside the *sector* list reads as a restriction on who may engage, which is not the intention and is not true. Whole-block replacement — the block is the unit check 12 protects. |
| 54 | `src/_data/copy/home.md` `instrument.body` | draft 5 §6.2 | New paragraph: the dimensions describe how a system behaves, not what industry it serves | §6.2 places this "after the four pairs, before the no-aggregate-score paragraph", and **two files match that description** — `oal.md`, where `dimensions.body` literally says "Four pairs" and `nototal.body` is literally the no-aggregate paragraph, and `home.md`, whose `instrument.body` is a dimensions paragraph followed by a no-aggregate paragraph. It is placed on **Home**, and the reason is a consequence rather than a preference. The paragraph is about where the practice *concentrates* — a statement about the business, not about the method — and the rubric is a methodology document that must stay sector-neutral, which is precisely what the paragraph asserts. Putting it in `oal.md` would also change the published rubric under an unchanged version number: the changelog's own test classifies wording that cannot move a score as **clarifying**, which is a v1.1, and no version bump was authorised here. Verified by drill rather than reasoned about — adding a clause to `dimensions.body` turns the new check-21 test red naming the divergence. Recorded here because two vault sources genuinely disagree and the resolution was mine. |
| 55 | `src/_data/copy/independence.md` `constraints` | draft 5 §6.3 | New constraint: **Scope of the method** — OAL assesses system behaviour and not clinical safety, medical-device or regulatory classification | Named refusals rather than a general disclaimer: DCB0129, DCB0160 and MHRA SaMD classification. §6.3's own wording is the argument — "we say so rather than scope around it". It sits with the other constraints because the page's structure is that the constraints are published in the order they bind, and a scope boundary binds like the rest. |
| 56 | `src/_data/copy/services.md` `review.addressed`, deleted; `src/services.njk` | draft 5 §6.4 | Card 2's closing paragraph removed in full, with nothing replacing it | The fragment and its template line are both gone — `frag` throws on a missing key, so a half-deletion would have failed the build rather than rendering an empty element, which is the intended behaviour. **Nothing fills the gap.** Card 1 ends on an italic, cards 2 and 3 end on their lists; that asymmetry is the state the pass-two build shipped and accepted, and the instruction not to re-treat the italics or rebalance is recorded in a comment at the deletion site so the next person does not tidy it. Note for accuracy: the paragraph was described as italic and was not — it rendered in `.note`, 0.82rem Archivo in `--slate`, with no `font-style` anywhere in its chain. |
| 57 | `src/_data/copy/home.md` `prices.note` → `basis.heading` + `basis.list`; `src/index.njk`; `.basis` in `src/styles.css` | draft 5 §6.5 | The block below the price grid becomes *About these prices* and three bullets | No rule above the heading: a free-standing rule on this site always means a scale, never a separator, so the heading carries the separation. The list class is spacing and nothing else. Currency is deliberately absent from bullet one — the footer already says "prices in GBP", and the same fact in two wordings on one page reads as inconsistency rather than emphasis; the deliberate repetition on this page is the fees-never-contingent line, which now appears both in `prices.body` and as bullet three, on purpose. **Three bullets, and the fourth is absent by decision** — see the flagged-and-left row below. |
| 58 | `src/_includes/layout.njk` footer; `site.json` gains `legalEntity` and `footerFields`, loses `country`; `vatNumber` token; `.legal` in `src/styles.css` | draft 5 §6.6, §4.4 | The footer becomes a **field list** carrying the VAT registration, and stops being a sentence | Structural, not stylistic. Terms and Privacy arrive as one field each once the entity exists, and that has to be a **diff rather than a re-layout** — the principle the signatory block was built on. Adding a field is now one line in `site.json` and moves nothing in the template; a field with an `href` renders as a link, which is how Terms and Privacy will arrive. `legalEntity` is **one record** holding name, number and jurisdiction together, so that changing the entity cannot happen without walking past the number — the population rule's shape applied to a fact rather than to a measurement, and the reason is that the registration belongs to the current legal person and does not survive incorporation while VAT68 is open. Only `vatNumber` renders. `site.country` is deleted rather than left unused: its sole consumer was the sentence this replaced, and an unused value is exactly how the retainer's price drifted out of the data in row 46. Separators are drawn in CSS because the `<ul>`/`<li>` structure already carries the semantics, so nothing reads a stray character between fields. |
| 59 | `src/_data/copy/services.md` new `grid.scope`; `src/services.njk` | draft 5 §6.7 | New prose under *Where to start*: one assessment covers one system, and multi-agent paths change scope rather than method | **Services only, and which page that is was established by measurement rather than assumed.** *Where to start* is `grid.heading` in `services.md`, pulled only by `services.njk`; Home's equivalent section pulls `prices.heading` — "What it costs" — from `home.md`, and each page binds exactly one copy file, so the key cannot leak. Home's bottom third therefore stays the grid and the basis block, literally intact, and no key had to be split. Set as a **bold run-in, not an `<h3>`**: a heading here manufactures a scan target next to a priced grid, and if a reader counts five products instead of four this has failed. No border, no rule, no price — the internal per-path figure stays unpublished and only the drivers are stated. |
| 60 | `src/_data/copy/services.md` `how.body` | draft 5 §6.8, §6.10 | New second paragraph — *we work read-only* — and "a named senior architect" becomes "the senior architect named on the scorecard" | The read-only statement is its own paragraph rather than appended to the fixed-scope sentence, because that paragraph's two clauses — fixed scope, named architect — belong together and must not be split by a third subject. The architect rephrasing closes an item open since pass one: "a named senior architect" sizes the practice to one of something, and the replacement claims no headcount in either direction while stating something the scorecard already makes true. The site must not claim a team it does not have, and must not read as permanently solo. |
| 61 | `src/_data/copy/independence.md`, the tracing-vendor sentence | draft 5 §6.10; supersedes flagged-and-left row A | *"whether their tracing vendor is independent"* → `{partyWord}` | **Row A is closed.** The same sentence appeared on About and Independence with different words — About using the token, Independence typing "independent" — and row A left it as a word-choice question rather than an engineering one. Draft 4's rule settles it: *third-party*, until the independence apparatus exists. Both now read from the same token, so the phase-2 flip moves both at once. Check 3's allowance is unaffected and was verified before the edit: the word survives in *The word we do not use*, where the section must say it in order to disclaim it, so the allowance still matches something and does not go stale — which would itself have failed the suite. |
| 62 | New `tests/checks/25-vat-registration.test.js` | draft 5 §4.4 | The rendered footer's VAT string must equal `site.json`, and no VAT-shaped literal may be typed anywhere else | The second half is the load-bearing one. **No check can know the number is wrong** — only HMRC knows that, and a check claiming to would assert something it cannot see — **but every check can know it was not supposed to be typed**, and that is fully visible from inside the repository. It matters because the registration expires with the legal person, and this repo has already shipped two single values restated somewhere nobody re-read: `CHANGES.md`'s "the repository is public" and `site.json`'s "not settled" publication date. **Deliberately no date-expiry**: a check that goes red on correct content is switched off inside a month, which is the check-12 lesson and applies exactly here. Frozen version directories are excluded because `/oal/v1.0/`'s footer is frozen at publication and has no VAT field — asserting one there would assert that a published document should have changed. Written red first and proved in **both** directions: typing the number into `layout.njk` fails naming the file and line, and pointing `site.json` at a different number fails naming both values. |

| 63 | `src/_includes/grid.njk` grid cell; `.grid .scope .sep` and the ≤46rem block in `src/styles.css` | draft 5 §5 | The meta line is composed from fields with a template-emitted separator, and each field takes its own line below 46rem | §5 asks for cells composed "from fields with template-emitted separators, not from one pre-joined string". Two defects were introduced doing it and both were caught by measuring rather than by review, which is the reason they are recorded. **Check 7 caught the separator at 1.37:1** — it was coloured `--untravelled`, which draws rules and fills, and row 5 moved text off that token for exactly this reason; a separator is text, and it now inherits `.scope`'s `--slate`. **The accessibility tree caught the spacing**: with the gap supplied by a CSS margin, hiding the dot at narrow widths left the two values touching, and the built page read `"Audit£2,500 + VAT1 week"`. Real whitespace in the markup survives `display: none`; a margin does not. The separator is `aria-hidden` because it is punctuation between two values rather than a word. Measured effect: the retainer cell's meta line drops from 82px to 61px at 320px. It does **not** reduce the table's width — see row G. |
| 64 | Two new tests in `tests/checks/23-measure-legibility.test.js` | draft 5 §5, and measurement | Check 23's collision detection is extended to the card headers and the grid cells | **§5's instruction could not be carried out as written, and that is the finding.** It says to "re-run check 23 at every breakpoint after the card headers grow — it is the only check that can see a header running under an adjacent element". Everything check 23 had selected inside `figure.measure`, and **`/services/` contains none**: measured, Home 1, `/oal/` 2, `/oal/v1.0/` 2, `/scorecard/` 8, Services **0**. Re-running it after the headers grew would have rendered Services four more times and inspected nothing — green because it was never looking, which is check 13's lesson one layer up. The detector now reaches `section.block` and every grid cell, at all four viewports, with a control that pulls a heading out of flow and asserts the collision reappears. **Its first run reported eleven collisions on a correct page, and neither cause was a defect** — both are recorded because both are ways a rectangle detector lies, and the measure arm had never met either since its parts are block-level siblings. A container always "overlaps" its own descendant (`.path` inside `.rowhead`, 53.9px × 125.7px on every render), fixed by taking only the innermost match — the same filter check 12 applies to prose. And `getBoundingClientRect()` on an inline element spanning two lines returns the **union** of its line boxes, covering space it never paints: `from £3,000/month + VAT` wraps after "from", so its rect swallowed the line beneath and reported a 17px × 119.8px clash with a sibling that is merely on the next line. Fixed with `getClientRects()`, one rect per line box. Neither is a weakening, and the control proves it. |

| 65 | `tools/freeze-version.mjs` (`FROZEN_UNIT`, `extractMain`, `writeManifest`); new `versions/v1.0/main.html`; `versions/v1.0.json` re-taken; new `versions/v1.0.published-index.html`; `eleventy.config.js`; `src/oal-version.njk`; `src/_includes/layout.njk` | the user's R1/R2/R3, 2026-08-12 | **The frozen unit is re-cut from the whole `index.html` to the `<main>` fragment plus the assets that render it.** `index.html` is no longer stored, pinned or in the manifest — it is rendered live, and its chrome tracks the site. | Pinning the whole document (row 50) froze the page's **chrome** along with its content. Measured 2026-08-12: eight of nine rendered pages carried the footer field list with the VAT registration and `/oal/v1.0/` carried the *launch* footer — a sentence this repository had already withdrawn. One site, two footers, and the frozen one advertising the site as it stood at publication. **This is not a re-freeze and no published content byte moved:** `main.html` is a byte-exact substring of the document that was published, whose sha256 is still `0289c300dd07…`. That document is retained beside the manifest and check 21 re-runs the substring comparison on every commit, so the claim is measured rather than asserted. `DEPLOY.md`'s record of the 2026-08-11 re-freeze as "not a precedent" is untouched. |
| 66 | New `tools/chrome-sheet.mjs`; `chrome.<sha>.css` emitted by `eleventy.config.js`; every page links it | R1 | A **derived, fingerprinted, chrome-only stylesheet** — masthead, version status strip and footer, every selector anchored to one of those. Loaded by every page including `/oal/v1.0/`, alongside that page's own frozen sheet. | R1 and R2 pull opposite ways on exactly one page: live chrome around frozen content. Two sheets in disjoint scopes is the answer. It is *derived* rather than hand-written because a second copy of the chrome's rules drifts, and when it drifts R1 fails silently — the frozen page keeps rendering last year's footer while the rest of the site moves. It declares **no `:root`** (two `:root` blocks merge rather than isolate, and the frozen `<main>` would take the live palette while every byte check stayed green) and **no `@font-face`** (a second copy of four byte-identical fonts is 123,008 B, which would put `/oal/v1.0/` 111.9 KiB over budget). Proved by drill 4: four live design tokens mutated, **0 of 2,160 computed-style observations inside the frozen `<main>` moved**, at 320/375/768/1280. |
| 67 | `src/_headers`, the `/oal/v1.0/*` block replaced | R3 | Cache-Control is stated **by type**, not by path: frozen and fingerprinted assets keep `immutable`, every HTML document revalidates. New explicit rule for `/styles.css`. | The old rule applied a year of `immutable` to an HTML **document** — measured on the wire, not read off the file. A purge cannot recall that from a browser that has already loaded it, so a redesign or a correction reaches nobody who has already visited. A path rule cannot tell a stylesheet from the page that links it. **Second finding, and one no source in this chain anticipated:** `/styles.css` is not fingerprinted and `_headers` declared nothing for it, so the zone default served it `max-age=14400` — four hours in which a redesign is invisible to a returning visitor, on **every page on the site**. The file's own comment claimed current paths took `max-age=0, must-revalidate`; that was true of HTML and false of the stylesheet, and nothing had ever read the response to find out. |
| 68 | `src/_includes/layout.njk`, `src/styles.css` `.vstatus`, `src/changelog.njk` + `copy/changelog.md` | ruled by the user 2026-08-12 | **A version status stamp**, rendered live from `oal.json` on every version page, outside `<main>`. A superseded stamp names and links its successor. The changelog's `Superseded` count derives from the record instead of being typed. | §3.6 asked where the stamp lived and what moving it cost. **It did not exist.** `v.status` reached exactly one place in the whole build — a `<td>` in the changelog's index table — so `/oal/v1.0/` told a reader its version, date, licence and permanent address and nothing about whether it was still the current one. A version page saying `Current` after v1.1 publishes is a published claim becoming false by mechanism. Drawing the freeze boundary is the only cheap moment to place it: inside `<main>` it would freeze with the content, and moving it later would be a version event over a label. Gate 0 also found the changelog rail's `Superseded: None` was **hand-typed**, so publishing v1.1 would have left two surfaces on one page disagreeing, both reading as authoritative. |
| 69 | New `tests/checks/27-one-chrome.test.js`, `28-html-revalidates.test.js`, `29-version-status.test.js`; `21-version-freeze.test.js` re-derived | R1, R2, R3 and check 16 | Three new checks, each with its denominator declared, each **written red-first and proven to fail against the build it was written against**. Check 21 goes from five tests to six; none was dropped. | 27: the chrome is one chrome on all 9 rendered pages, and the chrome sheet cannot reach inside `<main>` (measured against the rendered DOM, not argued from the source). 28: every HTML document is delivered revalidating with no `immutable`, plus a live arm requiring a **304**. 29: every published version page's rendered status matches the record and exactly one version is `Current`. Check 21's first test moved from hashing the build to hashing the stored unit — a manifest over the build would now go red on every legitimate chrome change, which is the un-editable-stylesheet failure (row 40) in a new place — and a sixth test holds the stored fragment to the published document. |
| 70 | `src/sitemap.njk` | drill 1, 2026-08-12 | `collections.all` is now sorted by URL. | Found by diffing two builds: `/oal/v1.0/` moved from fifth in the sitemap to last, with the URL set identical. `collections.all` is ordered by each page's date, and with no date in front matter Eleventy uses the source file's **birthtime** — so this file's output was a function of filesystem metadata rather than of source. Rewriting `oal-version.njk` gave it a new inode and moved it. A `git clone` sets birthtime to clone time for every file, so **a fresh checkout could not reproduce this build's sitemap at all.** Same family as the scorecard PDF (row 47), found the same way. Proved fixed by drill: recreating a source file with a brand-new birthtime no longer moves the output. |

| 71 | `src/changelog.njk`; `src/_data/copy/changelog.md` `rail.superseded` deleted; new `supersededVersions` filter in `eleventy.config.js`; `src/_data/oal.json` `$versionsComment`; check 29 test 3 rewritten + new control | pre-landing review, same session | **Two claims made in row 68 were false when it was written, and are now true.** The changelog rail derives its superseded list from `oal.json`; the dead copy fragment is deleted; the versions record declares its own conditional fields. Check 29's third test compares unconditionally. | Row 68 listed `src/changelog.njk` and `copy/changelog.md` in its *Where* column and `CHECKS.md` said check 29 "closes the hand-typed second surface". **Neither file had been touched.** The rail still rendered a hand-typed `None`, and check 29's third test guarded its comparison with `supersededCount === 0 && !declared.has('superseded')` — so the moment a version actually became Superseded, which is the only moment the rail can be wrong, both conjuncts went false and the comparison was skipped. Simulated: zero failures today *and* after v1.1, with the rail still reading `None`. A change record claiming a fix that does not exist, beside a check that switches itself off exactly when the hazard arrives, is the two failure shapes this repository is most written against — committed inside the change whose purpose was closing that class. Proved fixed by drill: with v1.0 superseded and the rail forced back to `None`, check 29 goes red naming the disagreement; reverted, green. The rendered output is unchanged today (`Superseded: None` either way) — the fix changes what can go wrong, not what is shown. |
| 72 | `tests/checks/27-one-chrome.test.js` — three new tests; `tests/checks/28-html-revalidates.test.js` — new local revalidation arm | pre-landing coverage audit, same session | A regression test asserting every `:root` custom property survives the chrome derivation; a control for `isChromeSelector`'s boundary rule; an assertion that every page links the derived sheet; and a local 304 arm. | **Row 66's own bug shipped guarded by nothing.** `declarations()` was fixed to strip comments before splitting, and the module's docstring then claimed the derivation *"now cannot"* drop a token quietly. Measured by reverting the one line: `--track`, `--surface` and `--p0` vanish again and **every build guard stays silent** — `undeclared` only fires when a kept chrome rule *references* a lost token, and none does. A guard written for a bug that does not cover that bug is this repo's recurring shape, one comment over. Also closed: `dropped[]` was computed, returned "so the caller can assert on it", threaded into `CHROME.dropped` and **read by nothing** — 124 selectors dropped per build with nothing asserting which; it is now asserted. And the ETag/304 branch added to `serve()` existed solely so the revalidation claim would not need a deployment, while the only code sending `if-none-match` was the live arm, which skips without `ORDOIA_LIVE` — the enabling code landed and the test it enabled was never written. A broken `chromeHref` would also have shipped an unstyled site with a green suite, because check 27's other tests read the DOM and check 17 is satisfied by `/styles.css` alone. All four found by the pre-landing audit, not by the author. |

| 73 | `tests/checks/27-one-chrome.test.js` completeness assertion rewritten; `tools/chrome-sheet.mjs` `SCOPE_ROOTS`; `tests/checks/28-html-revalidates.test.js` stale-validator arm; `tests/checks/21-version-freeze.test.js` new `extractMain` controls; `tests/checks/29-version-status.test.js` de-duplicated | pre-landing specialist review, same session | **Three guards written in rows 71-72 could not fail, and one shipped a real leak.** All four replaced and each drilled. | **(a)** The `dropped[]` assertion added by row 72 was *tautologically dead*: `dropped` is populated only from selectors that already failed `isChromeSelector`, so `dropped.some(isChromeSelector)` is false by construction — 124 dropped, 0 chrome-scoped. Replaced by the complement — every chrome rule the live sheet declares must appear in the derived one — and **that first replacement was ALSO insufficient**: a substring check stayed green when the at-rule branch was deleted, because `.masthead nav` and `footer .foot` also exist as top-level rules. Now counted per context; drilled, the drop reddens naming `@media print` (4 rules) and `@media (max-width: 46rem)` (3). Third attempt at one guard, and only the third one fails when it should. **(b)** `.skip` is a SIBLING of `.masthead` in `layout.njk`, so it received none of the re-homed tokens and its `background: var(--raised)` resolved against the FROZEN `:root` on `/oal/v1.0/` — the R2-into-R1 leak the module header says cannot happen. Scope roots are now derived from one list. **(c)** The local 304 arm echoed back the ETag `serve()` had just produced, so a 304 was guaranteed and nothing about the site or the host could fail it; a stale-validator arm now requires a 200, and the docstring states plainly that only `ORDOIA_LIVE=https://ordoia.com` is evidence about the real host. **(d)** `extractMain` — "the one function whose output becomes a permanent address" — had no control on either throw path. Writing one established that it *refuses* a `</main>` inside a comment rather than handling it, which is the correct conservative behaviour and the opposite of what its docstring implied. |
| 74 | `tools/chrome-sheet.mjs`, `src/_includes/layout.njk`, `src/styles.css`, `src/_includes/fonts.css`, `FONTS.md` | pre-landing specialist review | One measurement, five restatements, two mutually exclusive answers. Corrected to the delta and deferred the totals to check 17. | The @font-face page-weight counterfactual was stated as `139.9 -> 269.6 KiB` in `chrome-sheet.mjs`, `141.8 -> 261.9 KiB` in `styles.css`/`fonts.css`, `111.9 KiB over` in row 66 and `119.6 KiB over` in both `layout.njk` and the sheet's own emitted header — **which shipped the wrong number to every visitor on every page**. Measured: 145,242 + 123,008 = 262.0 KiB, 112.0 KiB over. The `269.6` came from a different counterfactual (loading the whole live sheet, which also duplicates the sheet bytes) and was copied across as though it were this one. Only the delta `+123,008 B` is durable; a page total moves whenever the document does, which is how `FONTS.md`'s `139.1 KiB / 10.9 KiB margin` also went stale the moment a second stylesheet was linked. |
| 75 | `eleventy.config.js` `requirePublishableVersion` docstring; `BRIEF.md` §11; `src/_includes/fonts.css` header | pre-landing specialist review | Three surviving statements of the pre-2026-08-12 model, corrected or marked. | `requirePublishableVersion`'s docstring still said `/oal/v1.0/` "is rendered from the live oal.json", that it is "self-contained in its assets", and that freezing content is "pass-2 work" — on a function whose body this branch changed one line below, and where the new `!isFrozen(version)` clause IS that pass-2 work. `BRIEF.md` §11 item 3 carried "frozen, self-contained and immutable" with nothing attached, 118 lines from the §5 superseded note, so a reader checking the deliverables list got the withdrawn claim unmarked. And `src/_includes/fonts.css` opened by saying "This fragment is inlined into styles.css" in the present tense, implying an automated relationship that has never existed — it has **no build consumer at all** and had already drifted from its twin, saying "five .buyerq pull-quotes" against the rubric's actual eight. Corrected, and the file now says what it is. |

| 76 | `tools/freeze-version.mjs` new `PUBLISHED_SHA256` + a refusal to overwrite the retained document; check 21's provenance test asserts the literal | pre-landing security review, same session | The published document's sha256 becomes a constant the freeze tool cannot rewrite, `storePublishedAssets` refuses to overwrite `versions/v1.0.published-index.html`, and the re-freeze refusal message names that file alongside the manifest and the pinned directory | **The provenance anchor was self-certifying.** `storePublishedAssets` overwrote the retained document unconditionally and `writeManifest` then recorded the hash of that same freshly-written file, so the documented override — delete the manifest and the pinned directory, rebuild, re-freeze — minted a NEW document and a NEW anchor in one command. Check 21 compared the file only against `manifest.publishedDocument.sha256`, a value rewritten in the same breath, and passed green. Nothing anywhere asserted the literal `0289c300dd07…`; it appeared only in prose and in the rewritable manifest. **The one artifact the entire re-cut rests on could be replaced without a single check going red.** Drilled: re-minting the document and the manifest hash together — self-consistent, exactly what the old test passed on — now turns check 21 red. |
| 77 | `src/_headers` `/oal/v1.0/*.css` → `/oal/v1.0/styles.css`; `/chrome.*.css` → `/chrome/*`; chrome sheet emitted to `_site/chrome/<sha>.css`; check 28 asserts its delivered value | pre-landing security + performance review | Both mid-pattern splats removed, and the chrome sheet's cache header is now measured rather than assumed. | Two problems with one shape. **(a)** A Cloudflare splat matches `/`, so `/oal/v1.0/*.css` and `/oal/v1.0/fonts/*` both match `/oal/v1.0/fonts/anything.css` — two Cache-Control declarations on one request, which comma-join on this host. No such file exists today, and the overlap detector could not have told us: `sampleFor` in `tests/lib/posture.js` renders each splat as a single `x`, so it compares `/oal/v1.0/x.css` against `/oal/v1.0/fonts/x` and finds no intersection. **The structural guard written for the 2026-08-09 comma-join defect is blind to exactly this shape** — recorded as an open finding about the check, not fixed here. **(b)** Cloudflare documents splats for the trailing position; a splat with a literal suffix after it is undocumented, and if the host ignores it the rule matches nothing and the fingerprinted sheet drops to the zone's 4-hour default, losing the entire point of content-hashing it, silently. Both rules now avoid mid-pattern splats, and check 28 asserts `/chrome/<sha>.css` is delivered `immutable`. |
| 78 | `eleventy.config.js` — `buildChromeSheet()` moved into an `eleventy.before` hook; `chromeHref` is now a function | pre-landing performance review | The chrome sheet re-derives on every build, including watch rebuilds. | `const CHROME = buildChromeSheet()` sat at config module scope, so `eleventy --serve` and `--watch` reused the process-start derivation forever. Measured: under `--watch`, editing `.masthead`'s padding updated `_site/styles.css` and left the chrome sheet at its old bytes, old fingerprint and old `<link>` on every page. **So this module's own claim — "a chrome edit in `src/styles.css` reaches `/oal/v1.0/` on the next build with nothing to remember" — was false in the dev loop, which is the only place a designer edits the chrome.** Derivation costs 0.67 ms per call, so hoisting it bought nothing worth having. |
| 79 | `tests/lib/harness.js` traversal guard | pre-landing security review | `full.startsWith(path.resolve(root))` → `path.resolve(root) + path.sep`. | A string-prefix test, not a path boundary: `_site` also admits a sibling named `_sitemap` or `_site.bak`. Pre-existing and test-only — the server binds an ephemeral local port and is never deployed — but it is one character of correctness on the guard that keeps a test server inside its own directory. |
| 80 | `tools/chrome-sheet.mjs` new `isUnscopedSelector` + `rehomeUnscoped`; `convert()` consults `REHOME` per selector rather than per prelude | red-team + design review, 2026-08-13 (both unrun at PR time) | Bare selectors are scoped to the chrome roots instead of dropped, and `REHOME` now fires on a selector inside a list. | **The chrome sheet was incomplete by construction and its guard was built from the same predicate, so it could not see the incompleteness.** `isChromeSelector` keeps a selector only if it *begins* with a chrome root, so `nav`, `a`, `a:hover`, `a[href^="http"]::after` and `:focus-visible` were all dropped, and `.rail` was dropped whenever it appeared as one of ten selectors in the shared utility rule rather than alone. On `/oal/v1.0/` those declarations were therefore being supplied by `versions/v1.0/styles.css` — **the live chrome rendered by a published document's stylesheet, which is this module's header claiming the impossible for the second time.** Measured by disabling the frozen sheet in-page: the masthead nav came back as **Source Serif**, the wordmark as UA-default link blue, every focus ring with it. Drilled with the red team's own probe — adding `letter-spacing: 0.3em` to the shared rule moved `.masthead nav a` from 56.5px to 97.3px wide on `/about/` and **not at all** on `/oal/v1.0/`, with the whole suite green. After the fix both render 97.3px and `footer .rail` reads 3.6px at both addresses instead of 3.6 against 5.1. |
| 81 | `tests/checks/27-one-chrome.test.js` new test — the chrome does not depend on a frozen version stylesheet | same | Renders `/oal/v1.0/`, disables that version's own stylesheet, and asserts no chrome element's computed styles move. | The guard for row 80, and deliberately **not** a cross-page chrome comparison: that comparison is green today and would have stayed green, because `src/styles.css` and `versions/v1.0/styles.css` are still near-identical down to a byte-identical `:root`. R1 does not only say the chrome matches today, it says it keeps matching after future chrome changes — the half no byte comparison reaches. This test takes its element set from the rendered DOM rather than from `isChromeSelector`, so it cannot inherit the predicate's blind spot, and it caught **49** borrowed properties where the design review's manual pass had found 18. Computed styles only, no geometry: rects jitter to ~0.1px between identical loads on this site and a geometry assertion would report its own instrument. Red-first at 49, green after row 80, and red again when row 80 is reverted with the drill still applied. |
| 82 | `tests/allowances.json` new `c27-frozen-page-owns-its-own-document-box` | same | The four remaining `.sheet` margin dependencies on `/oal/v1.0/` are recorded as a deviation rather than fixed. | The last dependency row 81 reports is `body { margin: 0 }`: disable the frozen sheet and body reverts to the UA default 8px, narrowing the page 16px, and `.sheet`'s auto margins follow from 222px to 214px. **This one is not a defect, it is R1 and R2 in genuine conflict at exactly one property.** Body's box sets the page width, which positions the chrome (R1's business) and also decides `<main>`'s width below the max-width breakpoint (R2's). Which requirement owns it is a decision the person who set both requirements has to make, so it is logged in the shape BRIEF.md §3 requires — dated, one line, and failing the suite the day it stops matching — rather than resolved by whoever happened to be editing the derivation. |
| 83 | `tests/lib/posture.js` — `sampleFor` replaced by `patternsIntersect`; controls in check 14 | red team, 2026-08-13 | Pattern overlap is now decided exactly, by a memoised product walk over the two token sequences, instead of by testing one pattern against a single sample string. | **Closes the open finding row 77 recorded about itself.** `sampleFor` rewrote every `*` and `:name` as the literal `x`, so it could only ever see a collision where a splat expands to one segment-internal token. Measured: it was blind whenever the collision needed a splat to absorb a `/`, to carry a literal suffix, or to meet a `:placeholder` — six real pattern pairs, all now controls. Blast radius as shipped was **zero**; no two real blocks collide today. But adding `/*.woff2` beside the existing `/fonts/*`, an unremarkable next edit, was drilled putting two comma-joined `Cache-Control` values on the wire *and* pinning a non-fingerprinted font `immutable` for a year, with the suite green. More sample strings would have been the same mistake with a longer list, so the grammar is decided exactly instead. |
| 84 | `tests/checks/29-version-status.test.js` — the surfaces loop no longer skips to `/changelog/` and `/oal/` | red team, 2026-08-13 | The population widens from 3 of 9 rendered pages to all of them. | The comparison was sound — changing the changelog rail's value turns it red — but the denominator had been drawn around the two routes where the bug was originally found, which is the same shape as the earlier version of this test that switched itself off when `supersededCount === 0`. Note on the drilling: the red team's own drill planted `<dd>None</dd>`, which is the **correct** current value, so it could not have failed either way; the finding stood only because the route restriction is readable in the source. Re-drilled with a diverging value planted on `/about/`, which now turns the test red and previously did not. |
| 85 | `tests/checks/28-html-revalidates.test.js` split `assets` into `chrome` + `assets`; `tests/checks/21-version-freeze.test.js` reads `TARGET` and declares `mayBeEmpty` when nothing is built | red team, 2026-08-13 | Two populations that were measuring on each other's behalf. | Check 28's chrome arm and its frozen-asset arm counted into one population, so `rm -rf _site/chrome` left the arm written to guard the derived chrome stylesheet passing green with no derived chrome stylesheet in the build — the three frozen files kept the population non-empty. Check 21's third test joined `REPO_ROOT` with a hardcoded `_site` while every sibling reads `TARGET`, so `test:empty` and `test:handover` counted it among their passes while measuring a directory the run was not pointed at. Fixing that alone moved the handover baseline from 9 failures to 10, which is not a licence to move a baseline: the real limit is that a handover has no build to compare stored bytes against, so it is now declared as an empty population with a reason and the baseline is 9 again. |
| 86 | Home, the instrumentation sentence | draft 6 §5.1 | *"OpenTelemetry tracing through the full agent path into Langfuse and Grafana"* → *"OpenTelemetry tracing through the full agent path"*. | Positioning, not tidiness. Naming two widely-known products stands the practice **in** observability, which is the category the practice deliberately does not occupy — assurance is the product, observability the method — and it reads as an endorsement against the no-commission constraint. An open standard is not a vendor, so OpenTelemetry stays. **Replaced as a sentence, not as a fragment**: the brief quoted one sentence of a four-sentence block, and swapping the whole block for the quote would have deleted *"Instrumentation is how we gather evidence — it is not what we sell"* — a positioning claim nobody asked to cut. The brief's own definition settles it, since check 12's protected unit **is** the sentence. |
| 87 | Services, the self-build concession | draft 6 §5.2 | *"Build the harness — you will build a better one than we would, and we would rather assess a system that has one."* → *"Build the harness — we would rather assess a system that has one."* | The concession was doing two different jobs and only one of them is true. A team that knows the business genuinely can write a better **eval set** — that claim is retained verbatim two paragraphs above, because it is about the cases. That they will build better **machinery** is not something this practice knows, and conceding it argues against the harness work the practice sells. `can` stays; the prediction goes. |
| 88 | Services, How we work | draft 6 §5.7 | *"led end-to-end by the senior architect named on the scorecard"* → *"led end-to-end by the named assessor who signs it, against a rubric published for anyone to check"*. | Closes flagged row C properly. *Assessor* is what the person does on an engagement, and the scorecard already names them; *senior architect* is a seniority claim the reader cannot check. **No credential language, and that is a ruling rather than a preference** — there is no chartered body, licence or exam in AI assurance, so *qualified* is unfalsifiable and an assurance-literate reader is trained to ask *qualified by whom, against what*. Competence is asserted here by checkability instead. Scoped to this file on purpose: `person` is retained in the independence constraints, on the scorecard and in **section I of the rubric**, which is frozen `<main>` prose — a repo-wide rename would have turned check 21 red on a document this batch is not about. A role cannot be responsible for anything. |
| 89 | Services, the scoping-call CTA | draft 6 §5.6 | *"Read the rubric first — most people who read it will never contact us, and that is the intention."* → *"Read the rubric first and score yourself — the call is shorter and more useful when you arrive knowing roughly where you sit."* | Same words as the rubric intro, opposite job. On `/oal/` the clause is a credibility device: the instrument is usable without us. On a call-to-action it tells a ready buyer not to convert. **The markdown link was kept and the brief's replacement text does not have one** — `[the rubric](/oal/)` survives, because the stated rationale is the discouragement and removing an internal route to the rubric from the services CTA is not a copy edit. Two sentences where there was one; re-measured at 320px. |
| 90 | About, the `Elsewhere` section; `src/_data/site.json` `elsewhere`; `eleventy.config.js` `elsewhereLabel` / `elsewhereUrl` | draft 6 §5.8 | The heading, its one sentence, the `<section>` in `about.njk`, the data record and both tokens are removed. About is one section shorter. | Nothing replaces it — no substitute link, no renamed heading over the same idea, no rebalancing of what remains. The data record and its two tokens go with the section rather than being left behind, on this file's own rule that an unused value is how the retainer price drifted out of the data (row 46). **Two inherited claims were false and are recorded as such:** there is no `#elsewhere` anchor anywhere, inbound or otherwise; and About does **not** end on *"The failures we look for are the ones we have had to fix."* — that sentence is in `intro`, the page's **first** section. The last section was `Pronunciation` before this edit and still is, so the removal changed the page's last line not at all. |
| 91 | Footer — `src/_data/site.json` `footerFields`, `eleventy.config.js` `footerLine()`, `layout.njk`, `styles.css` | draft 6 §5.4 | Six fields, email at position 2 with a `mailto:` href; the practice name becomes a field rather than a hardcoded first item; separators become template-emitted text nodes; the field list is filtered before rendering. | Four defects close on one rule, which is why it is a filter over the data rather than four conditions in the template: a `VAT registration no.` label outliving its value, a leading separator, a trailing separator, and the known empty-anchor defect — the last of which stopped being cosmetic the moment the array contained a link. **The emptiness test is applied to the token before substitution**, and that distinction is the point: testing the resolved string passes `VAT registration no. `, which is a published label with nothing behind it. Drilled by emptying `vatNumber` and rebuilding — the whole field leaves and the strip ends on *prices in GBP* with no dangling middot. The name became a field so that one list has one filter and one separator rule; outside the loop it would need the leading-separator case handled twice. |
| 92 | `src/styles.css` — `footer ul.legal` separator mechanism | draft 6 §5.4, measured | The separator is taken out of flow into the column gap and the list clips its own overflow. Emitted with the field it **follows**, not the field it precedes. | **A deliberate deviation from §5.4's letter, in service of §5.4's rule, and the measurement is why.** The strip wants **766px**; the column it sits in is **592px at 1280** and 272px at 320. It is short by 174px at the widest viewport this site supports, so it is two to four rows at every width and has never been one line — and Terms and Privacy widen it further. That kills both in-flow bindings: bound to the preceding field the separator trails every wrapped row, bound to the following field it opens every wrapped row in the left margin, and §5.4 forbids both. Out of flow and clipped, a separator can only ever appear **between two fields on one row**, which is what the rule asks for and the one arrangement that survives a field being added. Column gap 28px, separator offset 16px, clip edge 8px: a row-leading separator is clipped by 4px and a focus ring clears the same edge by 3px. Both are measured by check 30, not trusted. |
| 93 | `tests/checks/30-footer-field-strip.test.js` — new | draft 6 §5.4, §6.6 | The footer field strip gets a check. It had none. | Check 12's `PROSE` is `main`-scoped, check 23's selector lives inside `figure.measure`, and check 7 calls `getComputedStyle` with no pseudo-element argument — so the footer had **no contrast and no collision check at all**, and the separator was drawn with `content:` where check 7 is structurally unable to read its colour. That is how this project shipped a separator at 1.37:1 (row 49). The check reads text-node client rects rather than the template, so a separator form nobody has thought of is covered by construction, and it now asserts the **absence** of generated content rather than the presence of a rule. **Measured, not assumed: extending check 12 to the footer would protect zero units** — the strip renders 97 blocks into check 12's own selectors and not one field reaches the eight-word floor. Five drills, each red then green then red on revert: clip removed, binding reversed, `::after` restored, 44px target dropped, separator recoloured to `--untravelled`. |
| 94 | `eleventy.config.js` `substitute()` | check 30's unit test | A **known** token holding `null` or `undefined` now resolves to nothing instead of to the strings `"null"` and `"undefined"`. | Found by writing the test before the filter. `{ vatNumber: null }` published *"VAT registration no. null"* on every page and the build succeeded. An unknown token still throws, which is the case the vocabulary exists to catch; a known token with no value is a data question and resolves empty so the filter above can drop the field. The two were being handled as one. |
| 95 | `src/_includes/grid.njk` | draft 6, queued with the copy batch | `class="skip"` → `class="vh"` on the coverage grid's hidden corner heading. | `.skip` is the skip-link class and carries a `:focus` rule that reveals it at the top left of the page plus a print rule that removes it; it hid this heading only by the coincidence of both being off-screen, where `.vh` is the class written for the job. Harmless while chrome and content share a stylesheet and not harmless after: `.skip` is a chrome selector, so check 27 reported it reaching inside `<main>` on `/` and `/services/`, and a class shared between chrome and content is one the derivation must either carry into every frozen page or drop from one. Both informational notes are gone from check 27's output. |
| 96 | `tests/checks/25-vat-registration.test.js` | measured on this branch | The frozen-snapshot exclusion is removed. Denominator 8 → 9. | **Commit A invalidated this check's premise and nothing noticed.** The exclusion was correct while `/oal/v1.0/`'s whole `index.html` came from stored bytes recorded before the entity existed — asserting a VAT footer there would have asserted that a published document should have changed. Commit A re-cut the frozen unit to the `<main>` fragment, so the chrome renders live and the frozen page states the configured registration exactly like every other page — measured on the build before the exclusion was touched. It had stopped describing the site and started hiding a page from its own check, which is this repository's most-repeated defect in a new place. The frozen pages are now counted as their own declared population, so the fact that the footer reaches them is a number rather than an assumption. |
| 97 | `src/styles.css` + `src/_includes/grid.njk` — the coverage × depth grid below 46rem | draft 6 §8 | The grid reflows into stacked blocks instead of scrolling. Each cell becomes its own block carrying its depth label; the coverage head becomes the section heading above its cells. | **Closes flagged row G, and reverses row 14's reading of WCAG 1.4.10 for this one table.** Row 14 wrapped the grid in `overflow-x: auto` so the table scrolled and the page did not, which fixed the page and left the reader worse off: measured at 320px, scroller 272px against a table of 418px, so *Retainer* — name and price both — sat entirely outside the box at rest. Re-measured this session before the fix: **"Retainer" spans 339..402px against a 320px viewport, and "from £3,000/month + VAT" spans 339..492px**, with *Review*'s price 3px past the edge and the *Sustained* column label off-screen too. Narrowing cannot reach it — `from £3,000/month + VAT` is one NBSP-joined run at 136px and three depth columns plus a row head do not fit 272px at any type size this design would accept. After the reflow the table measures 272px inside a 272px box with nothing hidden, and the meta line returns to one line as §8 writes it, because the row is now full width. **No copy touched and no price string changed.** |
| 98 | `src/_includes/grid.njk` — `.depth` label in every cell | draft 6 §8 | The depth name is emitted into each cell as markup, `display: none` above 46rem and shown below. | The reflow hides `thead`, so without this the grid loses an axis and becomes a list of prices — and it would have done so while every co-visibility assertion went green. **Markup rather than `content: attr(...)`, deliberately:** generated content is not a DOM text node, so `getComputedStyle` cannot report its colour and check 7 is structurally unable to measure it, which is the hole that let a separator ship at 1.37:1 (row 49) and the reason §5.4 banned it in the footer. A label the reflow makes load-bearing has to be measurable. It is not copy — the string is `products.grid.columns`, the same array the header row prints, so the two cannot disagree. **§8 says "pure CSS"; this is one span of markup, and the disagreement is recorded rather than resolved silently.** No JavaScript, which is what that phrase is protecting. |
| 99 | `src/_includes/grid.njk` — `scroller--reflow` modifier | check 13, during this commit | The `overflow-x: visible` rule is scoped to the grid's own scroller instead of every scroller on the site. | **A regression I introduced and an existing check caught, which is the system working.** The rule was written unscoped; there are **fourteen scrollers on this site, eleven of them on the rubric page**, and it took the scroll container away from all of them while only the two grid ones had been reflowed. Check 13 reported 7px of page overflow on `/changelog/` — a version table this session never touched. My first diagnosis was wrong too and is recorded because it is the more useful half: a grep for `<div class="scroller"><table class="…"` found no scroller on `/changelog/` and I nearly concluded the rule was innocent. The changelog's table carries no `class` attribute at all. **The grep was the instrument, and the instrument was the thing that was wrong.** |
| 100 | `tests/checks/31-grid-covisibility.test.js` — new | draft 6 §8 | Denominator: the five products in `products.json`. At 320 and 375, every product's name and its price are on screen together, and both axes are still legible. | Written red-first and it failed on the real defect at both widths. The invariant is deliberately about the **reader** and not the layout, because check 13 asserts the page does not push sideways and passed throughout the period this defect was live — the scroller absorbed it, and the page genuinely did not overflow. Runs are located by searching the rendered text for each product's own name and its own rendered price through the same `renderPrice` the templates use, so nothing here selects on a class the fix also touches: had it asked for `.grid td .prod`, a reflow that stopped emitting `.prod` would have emptied the population and gone quiet. The second test is separate on purpose — a stacked layout that drops an axis passes every co-visibility assertion. Four drills: scroller restored (**green, and recorded as such — the rule is inert for today's content and kept for the failure mode**), reflow removed entirely (red on both), depth label hidden (red on axes), coverage head hidden (red on both). |
| 101 | `tests/checks/32-publication-path.test.js` — new | the open item recorded 2026-08-13, ahead of §4's exit | Denominator: the provenance artifacts compared across a refused command, and the manifest entries verified against the stored snapshot. The freeze tool is now exercised rather than only read. | `tools/freeze-version.mjs` is the only thing that writes a frozen version and **no test had ever run it.** Check 21 reads what it produced; nothing exercised it. That gap stopped being abstract when §4's exit was chosen, because a re-freeze runs straight through the one refusal nobody has watched fire — and before `b10fad8` the tool **rewrote its own evidence**, minting a new document and a new hash in one command with check 21 green over the result. Every test runs in a temporary directory holding a copy of the tool, which treats that directory as the repository: a test that ran the real CLI to prove it refuses would, on the day the refusal broke, destroy the artifact it was written to protect. Four drills. Deleting `main()`'s refusal turns it red; deleting both refusals re-froze v1.0 in the sandbox and **reported success**, which is the pre-`b10fad8` failure reproduced exactly; and a synthetic tool that prints the refusal and writes anyway is caught by the byte comparison, naming the anchor moving from `0289c300dd07` to `24f5cbc8dac2`. |
| 102 | `tests/checks/32-publication-path.test.js` — `realpath` on the sandbox root | measured while writing row 101 | The temporary directory is resolved through its symlink before the CLI is invoked. | Recorded because the check was **vacuous without it and looked fine**. `freeze-version.mjs` runs `main()` only when `process.argv[1] === fileURLToPath(import.meta.url)`, and on macOS `os.tmpdir()` is `/var/folders/…`, a symlink to `/private/var/folders/…`. Invoked through the symlinked path the two strings differ, so the module imported, defined everything, ran nothing and **exited 0** — against which "the CLI refuses to re-freeze" and "publishing with no build is refused" both pass, for the same reason and neither of them the right one. Caught by the control arm that requires the command to *succeed* once the manifest is removed. Fifth instance on this branch of a guard that could not fail, and the first one caught by its own controls rather than by a reader. |
| 103 | `tests/checks/30-footer-field-strip.test.js` — two tests added | `/ship` coverage audit, 2026-08-13 | `footerLine()`'s remaining branches, and the vocabulary throw that sits beside the coercion row 94 changed. | Found by auditing the diff rather than by a failure. **§5.4 says "fields with absent or empty `text` are dropped" and the ABSENT case had no assertion anywhere** — only the token-resolves-empty case did. Absent is the shape a hand-edit to `site.json` actually produces: `{ "href": "…" }` with the text forgotten renders exactly the empty anchor this filter exists to prevent. Seven shapes now covered, plus a null/undefined field list, because Nunjucks passes `undefined` when the key is missing and a build must not throw on it. Separately: **`substitute()`'s unknown-token throw was covered by no test in the suite**, and row 94 edited that function two lines from the throw. The two cases must stay distinct — a KNOWN token holding null resolves to nothing and drops the field, an UNKNOWN token fails the build — because collapsing them publishes a footer silently missing a field. Both drilled: making `resolve()` return the empty string turns the first red, making an unknown token resolve to `''` turns the second red. |
| 104 | `src/styles.css` — `footer ul.legal .sep` offset, and the comment above it | design review, 2026-08-13 | `margin-right` 1rem → 0.74rem. The separator is now centred in its trough, and the comment states measured numbers instead of assumed ones. | **The comment was wrong, and that is the larger half of this row.** It claimed the offset put the glyph "mid-trough, 12px clear either side". Rendered, it sat **7.8px from the preceding field and 16px from its own — a 2:1 asymmetry at every width** — because the figure was written from arithmetic that was never checked against a render. A comment asserting a measurement nobody took is the same defect as a document asserting a fix that does not exist, which this repository has shipped twice (rows 28, 30), so it is corrected in the open rather than quietly. After: 12px and 11.8px, measured at 320/375/768/1280; every row-leading separator still clipped, margin 8px → 3.8px; the focus ring still clears the same edge by 3px, drilled by reordering the fields so the address leads a row. **Nothing in the suite measures the centring** — check 30 measures the clip and the ring only — so the comment is the sole guard and says so. |
| 105 | `src/styles.css` — `.grid th.rowhead` colour below 46rem | design review, 2026-08-13 | The coverage head takes `--ink`; the depth label keeps `--slate`. | The reflow stacks the coverage head directly above the depth label, and both rendered at 11.52px uppercase `--slate` with identical letter-spacing — separated only by font-weight 500 against 400. **Two different axes of the same table, visually indistinguishable, at the one width where a reader has to tell them apart**, which turns the coverage × depth ladder into a flat price list on the width most first-time readers meet it at. Coverage is the parent of the cells beneath it, so it takes the darker token: 14.42:1 against the label's 6.21:1. Palette only — no new size, weight or rule. Check 31 asserts both axes are *legible*; it cannot assert they are *distinguishable from each other*, so that judgement is recorded in the stylesheet. |
| 106 | `tests/lib/visibility.js` — new; consumed by checks 30 and 31 | red team, 2026-08-13 | One shared oracle answers "can a reader see this run", by intersecting the run's rect with the clip region of **every** clipping ancestor, `overflow-clip-margin` included. | Checks 30 and 31 each grew their own answer and each was wrong in a different direction, so the answer is now written once. Deliberately **not** `elementFromPoint`: that asks "is this the topmost hit-testable thing at this coordinate *in the current scroll position*", which makes the verdict depend on scroll and z-order. A run below the fold is not hidden, it is further down. Intersecting clip regions is scroll-independent, which is the property both checks actually need. |
| 107 | `tests/checks/30-footer-field-strip.test.js` — `visible()` | red team, 2026-08-13 | Visibility is decided in-page against real clip regions instead of by comparing against `ul.getBoundingClientRect()`. | **Instance seven of the pathology, and the sharpest yet.** The filter treated the list's *border box* as the boundary of what a reader sees. That is true for `overflow: hidden` and false for `overflow: clip`, which paints outside the border box by design. Drilled: `overflow: clip; overflow-clip-margin: 12px` and every row-leading separator renders, is hit-testable, opens every wrapped row — and the guard written to catch exactly a dangling separator stayed **8/8 green**. A geometric proxy for a clip is not the clip. Re-drilled after the fix: the same mutation now turns the check red. |
| 108 | `tests/checks/31-grid-covisibility.test.js` — `onScreen`, plus a new `coVisible` | red team, 2026-08-13 | The predicate now requires the run to be unclipped, and co-visibility is a property of the PAIR — their combined vertical extent must fit one screen. | **The check never asked its own question.** `onScreen` tested the horizontal axis only; `LOCATE` captured each run's `top` and nothing ever read it. Drilled: `max-height: 13rem; overflow-y: hidden` on the reflowed scroller put four of five products off the page, no scrollbar, unreachable — and check 31 stayed 3/3 while the full suite stayed **byte-identical to green with 80% of the price ladder invisible**. Note what the fix is *not*: requiring both runs inside the initial viewport would fail on every page where the grid is not the first thing, because a page scrolls. Unreachable is the defect; further down is not. Re-drilled: the same mutation now turns both arms red. |
| 109 | `tests/checks/31-grid-covisibility.test.js` — the `together` pairing | red team, 2026-08-13 | A name run and a price run must be co-located — same grid, same row — not merely both present. | `LOCATE` computed a `grid` index for exactly this purpose and **nothing read it**, which is the `dropped[]` shape again: a field produced to make a guard look thorough and then discarded. `renderPrice` does return the identical `£2,500 + VAT` for both `audit` and `top-up`, so the collision is real. **But the red team's "the aliasing is live" overstates it, and I could not reproduce a verdict change:** top-up's name and price share one text node (`↓ Baseline top-up · £2,500 + VAT · ~1 week`), so they are always hidden or shown together and loose pairing never rescues it. Fixed as a correctness matter with the discrimination **unproven**, stated here rather than claimed. |
| 110 | `tests/checks/31-grid-covisibility.test.js` — `LOCATE` offsets | red team, 2026-08-13 | The match offset is computed on the RAW node text, with a whitespace-tolerant regex fallback, instead of on a normalised copy. | An index taken from a whitespace-COLLAPSED string was applied to the raw node via `setStart`/`setEnd`. Correct only while every text node happens to be tight, which is true today and one template reindent from false: with the node reindented, the Range selected `"ne top-up · "` and the check measured those twelve characters as the price. The `try/catch` could not save it — collapsing only ever shortens, so `setStart` never throws, and the arm was **dead by construction**, its fallback being the whole-cell rect the file's own header says the check exists to avoid. |
| 111 | `tests/checks/30-footer-field-strip.test.js` — `lines()` and its controls | red team, 2026-08-13 | Rows are matched against their SEED extent, which never widens. A control now plants a bridging run and requires two rows to survive. | Single-linkage clustering with a mutating extent: each merge widened `row.top`/`row.bottom`, so one run overlapping both sides of a gap chained two genuinely non-overlapping rows into one. The control pinned merging as **correct** and had no case where merging is wrong — the same non-discriminating shape as the five guards already rewritten on this branch. Verified both ways before the control was written: widening gives 1 row and no dangler, seeded gives 2 rows and finds `ends`. The bridging run needs only a taller line box than its neighbours, which is the exact condition (the 44px email link) the grouping was built to tolerate. |
| 112 | `tests/checks/30-footer-field-strip.test.js` — new test; `src/styles.css` comment | red team, 2026-08-13 | A test that actually measures the clip clearance and the focus ring, and a comment that no longer claims a test already did. | **The comment asserted "Both are measured by check 30 rather than trusted" and check 30 contained zero references to `outline`, `focus`, `padding-left`, `column-gap` or `margin-right` — it measured neither.** It also named a 4px clip clearance where the real figure was 8px. Drilled: shrinking the list's padding to `0.125rem` pushes the focus ring 1px past the clip edge at 320, 375 and 768 under 200% text zoom — WCAG 1.4.4 and 2.4.11 — with the full suite green. The two clearances pull in opposite directions, so they are asserted together as signs: the separator must be clipped, the ring must not be. |
| 113 | `tests/checks/32-publication-path.test.js` — the sandbox control | red team, 2026-08-13 | The control asserts the sandbox's VALUE — the copied tool's resolved `FROZEN_DIR`, plus a before/after hash of the real repo across two CLI runs — instead of regex-matching the shape of the `REPO_ROOT` expression. | The sandbox is genuinely complete, but the control certifying it could not detect an escape: it pinned the FORM of `path.resolve(fileURLToPath(import.meta.url), …)` while ignoring the second argument, and its "prove it positively" step tested a write **the test itself performed**, with the tool never involved. Drilled: `'../..'` → `'../../..'` relocates the whole sandbox to `os.tmpdir()`, outside the tree `cleanup()` deletes, so every write leaks and persists — and this control passed. Re-drilled after the fix: the same one-token change now turns it red. The import guard also now covers `./sibling`, not only `../parent`. |
| 114 | `src/_data/copy/oal.md`, rubric intro — **and v1.0 re-frozen** | draft 6 §5.3, §4 exit 2 | *"…the rubric is written to be used without us, and most people who read it will never contact us. That is the intention."* → *"…the rubric is written to be used without us."* The comma becomes a full stop; nothing replaces the clause. | **A version event, and the user's call, taken as exit 2 — re-freeze v1.0.** The full sentence begins earlier than the project record ever captured it, which is why §5.3 insisted the line be read before editing; it was. The edit turned check 21's fifth test red by design and nothing else — 6 of 7 green, naming *"v1.0 reads differently at /oal/ and /oal/v1.0/, from word 126."* The re-freeze followed the documented override: delete the manifest, the pinned directory **and** the retained document, rebuild, re-freeze, update `PUBLISHED_SHA256` in a reviewed diff (`0289c300dd07…` → `da0ed36ecf24…`). §3.4.6's trap was drilled first — the build serves stored bytes, so a regeneration can silently re-record the OLD fragment and report success; measured after deleting them, `_site/oal/v1.0/` regenerated from live source with the clause gone. `main.html` 42,494 → 42,420 bytes and remains a byte-exact substring of the retained document. |
| 115 | `tools/freeze-version.mjs` `PUBLISHED_SHA256` + its comment | row 114 | The anchor literal moves to `da0ed36ecf24…`, with what did and did not move recorded beside it. | **The first draft of that comment claimed the frozen `styles.css` was byte-identical across the re-freeze. It is not, and the measurement caught it.** A re-freeze copies the CURRENT `src/styles.css`, so the frozen sheet now carries this session's design work rather than 2026-08-11's. That is R2's exact hazard — a redesign reaching a published document — so it was measured rather than argued: **0 computed-style differences across 45,444 values** inside the frozen `<main>` at four widths against the pre-session build. This session's CSS touched the footer (chrome, outside `<main>`) and the coverage grid (not on this page). The 2,033 geometry deltas are all downstream of the intro paragraph losing a line, which is the edit. Fonts and favicon are byte-identical. |
| 116 | `DEPLOY.md` — *"not a precedent"* withdrawn; new section *When a published version is re-frozen* | row 114, and §4's requirement that the withdrawal be a deliverable of exit 2 | The sentence recording the 2026-08-11 re-freeze as "not a precedent" is withdrawn. A new section states what the practice is, with both re-freezes tabulated and four costs listed. | Two re-freezes is a practice, not an exception, and **a document claiming a constraint the repository does not observe is worse than one merely out of date** — the standard rows 28, 30 and 42 already set. The fourth cost is new and was found by measuring this deploy: `/oal/v1.0/styles.css` is served `immutable` for a year at a **stable, unfingerprinted URL**, so a visitor who loaded the page before a re-freeze keeps the old sheet and no purge can reach them. Measured for this deploy — new document against the old cached stylesheet, **0 computed-style differences across 45,444 values** — so it is safe, but safe because of what this session happened to change rather than by mechanism. The next re-freeze must fingerprint the frozen stylesheet or repeat that comparison. |

| 117 | `.github/workflows/ci.yml` — new | user, at land time | Pull requests now run the check suite. All four targets, with the handover and empty baselines asserted as numbers. | **This repository ran no checks on a pull request at all.** `deploy.yml` fires on `push: branches: [main]`, so the suite ran for the first time *after* a merge had landed — a red run meant a second commit rather than a blocked merge, and every gate before that point was a human remembering to run `npm test`. Four targets because `npm test` alone is not this suite's gate: `build` and `live-local` must pass, and **`handover` and `empty` are asserted by failure count** (9 and 62), because a suite whose empty-target run quietly starts passing has stopped measuring anything — the exact failure rows 31-35 record. Moving a baseline is a deliberate act in a reviewed diff, the rule `PUBLISHED_SHA256` already follows. **The first version of the count parser always read 0** — node's spec reporter prefixes its summary with a multi-byte `ℹ` and awk's `.` matches one byte — so it would have failed every run: safe, but useless. Caught by running it against real output *before* committing, and the controls now pin all three modes: baseline moved, checks gone quiet, suite produced nothing. |

| 118 | `tests/checks/23-measure-legibility.test.js` — the run-under control's planted CSS | **the new CI, on its first run** | The plant co-locates a heading with its next sibling instead of nudging it down a fixed `2.2rem`. | **Found by row 117's CI within minutes of existing, which is the argument for row 117.** The control passed on macOS and failed on Linux: 11 movable sections, **0 collisions reproduced**. The detector was not blind — the *plant* was missing. A fixed `2.2rem` nudge only lands on the paragraph beneath if the headings above it have the heights the environment happens to render, and the designer handover pulls off-origin webfonts (which is why check 6 fails on it) that do not load in a runner, so everything fell back to different metrics and the heading landed in empty space. Pinning the heading and its next sibling to the same origin makes the overlap true **by construction** — two text runs at the same top, in the same box, at any font size anywhere. Drilled after the change: blinding `collisions()` turns the control red on both the build and the handover, so the plant became deterministic without the assertion becoming weaker. A control whose reproduction depends on font metrics is a control that tells you about the runner. |
| 119 | Home and Services, coverage × depth grid — `src/styles.css`, new `.grid .depth { display: none }` | **the user, reading the live site** | The in-cell depth label is hidden above 46rem, where the column header already carries the axis. | **Broken rendering, fixed — and the same defect as row 13, made a second time.** `grid.njk` has said since draft 6 that this label is *"`display: none` above 46rem, where the column header carries it and this would be a duplicate in the accessibility tree"*. The rule was never written. `.grid .depth` existed only inside `@media (max-width: 46rem)`, so above 736px the span rendered as an ordinary inline box hard against the text node beside it and the two empty cells published **`Testednot offered`** and **`Sustainednot offered`** on both pages. The five populated cells printed their own column header again directly above the product name, and a screen reader said "Tested … Tested not offered". The designer handover settles the intended rendering and carries no `.depth` span at all (`index.html` L122). Row 13 was `.selfcheck .eyebrow` inline against its own question; this is the same shape, the same repair, and it reached production because nothing measured it. **And it had already falsified a published document:** `RATIONALE.md` row 9 states *"the empty cells now read 'not offered'"*, and for the whole of draft 6 they read `Testednot offered`. That sentence needed no edit — the fix makes it true again — but a rationale the site contradicts is exactly what this table exists to catch, and no check compared the two. |
| 120 | `tests/checks/33-text-abutment.test.js`, `tests/lib/overlap.js` `abuts()` — new | row 119 | Two words never print on the same line with a letter or digit on both sides of the junction and no space between them. Site-wide, at 320 / 375 / 768 / 1280. | **No check in this suite opened this grid above 736px.** Check 31 measures the same table and measures it at 320 and 375, where `.depth` is `display: block` and nothing runs together — its logic was never wrong, its *population* excluded the only widths at which the defect existed. Drilled three ways: breaking the fix turns it red; **removing 768 and 1280 while leaving the defect in place turns it green**, which is check 31's blind spot demonstrated rather than asserted; and the alternative repair — a literal space in the template, label still visible — also goes green, because the invariant is what a reader sees and not which rule was written. Run site-wide it first reported the two real cells **and fourteen correct-English junctions** (`<strong>finding</strong>. A score…`); narrowing the predicate to a word-to-word junction removed all fourteen and kept both, which is a better trade than narrowing the population to one table and calling the rest unmeasured. The hole that leaves — a run ending in punctuation cannot open a finding — is stated in `abuts`. |
| 121 | `.github/workflows/ci.yml` — both baselines | measurement | `HANDOVER_EXPECTED_FAILURES` 9 → 10, `EMPTY_EXPECTED_FAILURES` 62 → 64. | The empty move is arithmetic: one new site-touching test, one new control that never reaches the site. **The handover move is the finding.** Check 33 goes red on the handover because the handover prints *"Self-checkIf you deleted the sentence…"* on all eight rubric dimensions — which is **row 13**, recorded and fixed in the build five days before this check existed. The check was not told where to look and rediscovered it from the rendering. That is the second time this baseline has moved and the second time the move was the evidence a check worked; the first was check 23 at 8 → 9. |
| 123 | `tests/checks/33-text-abutment.test.js` — new third test, the accessibility arm | the `/ship` coverage audit | The `not offered` cells' accessible subtree must name the depth exactly once across the site: never above 46rem, where the column header carries it, and always below, where the reflow drops `thead`. | **The abutment test is structurally unable to cover this, and the drill proves it.** `abuts` measures what a reader *sees*, which is why it accepts either repair — so the space-repair (label visible, literal space in the template) turns it **green while a screen reader still hears the depth twice**. Run against the defect: abutment green, this red. Measured at 1280 rather than reasoned about — fixed subtree `["not offered"]`, defect subtree `["Tested", "not offered"]` — which is what turns `styles.css`'s "says it twice to a screen reader" from an assertion into a measurement. `interestingOnly: false` is required: with the default a `<td>` carrying no accessible name of its own returns an **empty** subtree, and this check would have passed against nothing on every page — lesson 8, one option flag away. |
| 124 | The same check's depth comparison | its own first run | Case-insensitive. | **Found by disbelieving a finding.** The first version compared the accessible names against `products.grid.columns` verbatim and reported *"announces no depth at all"* on every render below 46rem — where the label is present and correct. Chromium reports the **rendered** text in the accessibility tree, so §8's `text-transform: uppercase` makes the node read `TESTED` while `products.json` says `Tested`. The narrow arm could only ever report a false finding, and the wide arm matched **by luck** — there is no transform above the breakpoint, and the day the design uppercased that label the leak check would have gone silently blind instead of red. |
| 125 | `tests/checks/33-text-abutment.test.js`, `tests/lib/overlap.js` — four hardening fixes | **the adversarial pass**, on a check that had already been drilled three ways and reviewed inline | (a) the accessibility arm reads the header's **text per column**, so it asserts *exactly once* rather than *at most once*; (b) a new controls test parses the reflow breakpoint out of `styles.css` and asserts `VIEWPORTS` brackets it; (c) the declared population is `junctions`, counted after the same-line test; (d) `abuts` consumes `startsWord`/`endsWord` resolved against the **painted** edge instead of parsing `text`. | **Three of the four were in guards the inline review had added an hour earlier, and each was proven by drilling rather than argued.** Emptying every `<th scope="col">` removed the depth axis from the site above 46rem and the suite stayed **119 pass / 0 fail**. Restoring the defect *and* cutting `VIEWPORTS` to check 31's pair passed every `ci.yml` gate at its committed number, because the handover's abutment findings fire at all four widths so the failure count cannot move — a count cannot defend a population. `pairs` counted 557,065 scan candidates of which 499 were on the same line, so losing every junction on the site moved the denominator under 0.1%. And `<span>Audit </span><span>not offered</span>` in an inline-block renders `Auditnot offered` while `text.slice(-1)` sees the space: **the control written to protect that case asserted the wrong way round and guaranteed the detector could not fire.** All four now drilled red-then-green. |
| 126 | `CHECKS.md` Baseline A paragraph | the same pass | `127/127/0/0` → `129/129/0/0`. | The one number in the re-measured paragraph that had not been re-measured, in the paragraph whose subject is stale numbers. It was the previous session's figure carried forward with part of check 33 added. A re-measurement is not a re-measurement until every number in it has been measured. |
| 122 | `src/styles.css` page weight | measurement | +349 B gzipped, all of it the comment above the new rule. | Recorded because `/oal/` is the tightest page on the site: **151,052 B against §4's 153,600 B budget, 2,548 B of headroom**, measured with this change in and check 17 green. A twelve-line comment is 14% of what is left. Stated so the next person adding prose to this stylesheet knows what it costs rather than finding out from check 17. |
| 127 | Home and Services, coverage × depth grid — `src/_includes/grid.njk` two spans per cell, `src/styles.css` `.grid .depth--wide` / `.grid .depth--narrow` | **the owner, reading the live site** | The in-cell depth label prints again above 46rem, above the product name and price, **and the literal space goes in**: the empty cells read `Tested not offered` and `Sustained not offered`. | **Row 119's repair is reversed; row 119's finding is not.** The abutment was real and the space is still required — what changes is which repair carries it. The owner looked at the result of hiding the label and ruled the other way: the depth belongs inside the cell, above the price, for clarity. That overrides the designer handover, which carries no `.depth` span, and it is the owner's call to make about their own commercial instrument. **Two spans, because `aria-hidden` is a static attribute and the column header's presence is not** — above 46rem `<th scope="col">` speaks the depth and an in-cell copy is a duplicate; below it §8's reflow drops `thead` and the cell is the only carrier. One span cannot be silent at 1280 and spoken at 320. Measured before the design was chosen, against the exact call check 33 makes: Chromium excludes `aria-hidden` subtrees from `accessibility.snapshot({ interestingOnly: false })` — at 1280 the cells return `["Audit"]` and `["not offered"]`, at 320 `["INSPECTED Audit", …]` and `["TESTED not offered", …]`. **Both spans keep the shared `depth` class deliberately**; see row 130. Also corrected: row 119 says "seven cells" and "the five populated cells". `products.json` gives three depth columns over two coverage rows, so it is **six cells per grid, four populated** — twelve across the two pages, which is what check 33's new arm reports. |
| 128 | `tests/checks/33-text-abutment.test.js` — new second test, the visible arm; `COLUMN_ORACLE` extracted and shared | row 127 | Every body cell of the grid visibly prints the depth it belongs to, at 320 / 375 / 768 / 1280. | **The owner's preference had no check behind it, and a comment is exactly what failed last time.** Without this, the next person to read `grid.njk` re-applies `display: none` and every other test in the file stays green — which is how row 119 happened, five days after the comment describing the missing rule was written. Located by **text, not by class**: asking for `.depth` would empty its own population the moment the class is renamed, which is the rule check 31's header already states. Case-insensitive on the source text, for row 124's reason. Written red-first against the pre-change build: **24 findings, six cells on each of two pages at 768 and 1280, and silent at 320 and 375** — the exact absence the owner asked to have reversed. **Four drills, and B and D isolate the two cell-level assertions from each other:** (A) remove the space → abutment red *alone*; (B) remove `aria-hidden` → accessibility arm red *alone*, "hears the depth twice"; (C) delete the narrow span → **both** red, correctly, since that span carries the visible label *and* the announcement below 46rem; (D) re-hide the wide span → visible arm red *alone*. `COLUMN_ORACLE` is shared page-side source rather than a second copy of the derivation, in the manner of `CLIP_ORACLE`: two copies is the "two surfaces that can disagree" shape, and the way this check starts asserting one cell's axis against another cell's header. It skips the handover for check 31's reason, so **it can never earn a handover-baseline move** — stated in the file, because a check that cannot produce that evidence should say so rather than look like one that has. |
| 129 | `.github/workflows/ci.yml` — `EMPTY_EXPECTED_FAILURES` | measurement | 64 → 65. `HANDOVER_EXPECTED_FAILURES` **held at 10**. | Arithmetic: one new site-touching test, which goes red against an empty target because `survey()` refuses to report on a population of zero. **The handover number holding is the assertion, not the absence of one.** The new arm skips the handover, so a move there would have meant the skip was not taking and the check was measuring the handover's hand-authored grid against a product record that postdates it. Measured on all four targets rather than derived: build **130/120/0/10**, live-local **130/128/0/2**, handover **130/79/10/41**, empty **130/55/65/10**. |
| 131 | `tests/lib/visibility.js` `__clipRegion`; `tests/checks/33-text-abutment.test.js` — eight hardening fixes to the guard added in row 128 | **the adversarial pass**, on a guard written red-first and drilled four ways an hour earlier | (a) the clip oracle fails closed on any non-`none` `clip-path`; (b) `MIN_INK` rejects a label under 6px painted; (c) the path row is excluded by its index in `products.json`, not by matching `.path`; (d) `VIEWPORTS` pinned by value and the controls test reads `min-width` as well as `max-width`; (e) the cell count is pinned per render and the grid-page count asserted; (f) visible occurrences are counted, so twice is a finding; (g) only a text node that IS the depth name can credit a cell; (h) the credited run must be topmost in its cell. | **Four of the eight held all three CI gates at their committed numbers while the depth label was gone, doubled or unreadable** — `npm test` 130/120/0/10, handover 10, empty 65, with the owner's ruling visibly broken on the page. The pathology is the one this file keeps recording, this time on the guard written to end it: **the predicate was sound and the population was undefended.** `clip-path: inset(50%)` is one declaration, it is not `overflow`, and `__clipRegion` read only `overflow` — the label vanished from all six cells on both pages and the check said nothing. `tr.querySelector('.path')` matches any descendant, so one `<span class="path">` in a coverage rowhead took that row out of **both** cell-walking arms, 48 cells to 24, after which the user's original defect could be restored in silence. The controls test written to defend the viewport list asserted only that *some* viewport sat either side of the widest `max-width`, so the list could be halved and an ordinary `@media (min-width: 60rem)` rule could hide the label on every desktop width with that control green. Deleting `{{ grid(products) }}` from `services.njk` removed half the population and `survey()` cannot see half. All eight re-drilled red and restored green; the four targets are unmoved, so **none of this cost a baseline** — the hardening is entirely in what the check can see, not in what it counts. One hole is left open and named in the file: occlusion by an opaque sibling, which needs `elementFromPoint` and would cost the scroll-independence `visibility.js` deliberately buys. |
| 130 | Deployment posture for this change | measurement, and the owner's decision | The ≤4-hour stale-stylesheet window is **accepted rather than closed**, and the shared `depth` class is what bounds it. | `/styles.css` is unfingerprinted, and measured on the wire it is served **`public, max-age=14400, must-revalidate` while `_headers` L138 declares `max-age=0`** — the rule is present and the host is not honouring it, which is worse than the open item recorded. HTML is unaffected (`max-age=0`, `cf-cache-status: DYNAMIC`, never cached by the zone); the stylesheet is `REVALIDATED`, so the zone's browser TTL reaches it. What a reader holding the old sheet against this markup sees was **rendered rather than reasoned about** — the shipped `_site/index.html` with `main`'s `src/styles.css` inlined in place of the link: **above 736px** the old `.grid .depth { display: none }` hides *both* spans and the cells read `not offered`, the change simply not yet arrived, benign; **below 736px** the old `@media` rule shows both and the cells read `TESTED | TESTED | not offered`, self-healing within four hours. Keeping the shared class is what buys the benign half; without it both spans render unstyled at every width. Two alternatives were costed — a visually-hidden-twin markup with no visible failure mode, and fixing the zone setting then waiting out four hours before deploying — and the owner chose to ship. Recorded so the next person seeing a doubled label on a phone finds the reason rather than a bug. **Page weight moved the right way**: `/oal/` 151,052 → **150,985 B against 153,600**, headroom 2,548 → **2,615 B**, because the two replacement comments are shorter than the one they replace — the tightest page on the site got lighter while gaining twelve spans it does not render. The derived chrome sheet is unchanged at `3bfb239e.css` and check 21's byte-identity on `/oal/v1.0/` is green, so nothing reached the frozen document. |
| 132 | `src/_data/oal.json` `versions[0].body` — the v1.0 changelog entry | rubric §H's own internal note, and the owner's decision to amend v1.0 in place | The entry gains the amendment record: seven criteria tightened on 2026-08-15, each named with its dimension and level, and the statement that at least four of them could have moved a score and would have been breaking had the version been in effect. | **Written before the edit it describes, which is the point of it.** Rubric §H says *"the first breaking change will be tempting to classify as clarifying, because the classification is self-applied and the incentive runs one way. Write the reason in the changelog before making the edit, not after — if the reason cannot be written honestly, the classification is wrong."* So this is a separate commit that lands ahead of a single rubric byte moving, and the honesty test was taken while backing out was still free. Two things it deliberately does **not** do. It does not repeat the catalogue's clarifying/breaking labels: on audit, four of the seven fail §H's own test, which is symmetric — *"scored differently"*, not *scored lower* — and tightening 2 adds a metric-sink category, tightening 3 bounds a previously unbounded set in the direction that can raise a score, and the seventh adds re-ranker and scorer models to an em-dashed enumeration that reads as exhaustive. And it does not move `classification`, which stays `clarifying` because that field classifies v1.0 against its predecessor and there is none; the body carries what actually happened. The entry renders through `{{ v.body }}` at `src/changelog.njk:41`, which is neither markdown-rendered nor multi-paragraph, so the record is one paragraph by the template's existing contract rather than by adding support for a second — the same reason `v1.0.1` was declined on 2026-08-12. Chrome only, no version event: measured at **130/120/0/10**, the committed baseline, unmoved. |
| 133 | `tools/frozen-render-diff.mjs`, `tests/lib/computed-style.js` — both new | `DEPLOY.md:652`, which required this before the next re-freeze | A computed-style comparison of the frozen `<main>` at 320/375/768/1280, isolating the stylesheet by intercepting its response and substituting the candidate bytes. `--self-test` proves both arms. | **The precondition existed only as prose.** `DEPLOY.md` cost 4 said that before the next re-freeze one must "either fingerprint the frozen stylesheet or run that same comparison" — the 45,444-value measurement of 2026-08-13. Neither was in the repository: no script produced that figure, `45,444` appears only in prose, and the sheet was still unfingerprinted. The variable is isolated by **route interception rather than two builds**, because a version event changes the rubric's words in the same commit series that changes the stylesheet, and a build-to-build comparison reports both without being able to tell them apart. Geometry is deliberately outside `VISUAL_PROPS` for the same reason — it moves whenever the text moves, which is exactly what a version event does. The oracle is shared with check 27 rather than copied into it: those two measure complementary populations of the same thing, and 2026-08-13's `clip-path` blindness in checks 30 and 31 was two copies of one predicate. Self-test: identity reports 0 over 103,872 values, one planted `main { letter-spacing }` reports 1,800. **Measured for this re-freeze: 0 computed-style differences, 541 elements, four widths** — so the `.grid .depth--*` drift from `47d216b` does not reach the rubric page. |
| 134 | `tools/freeze-version.mjs` `stylesheetFile`/`builtStylesheet`, `eleventy.config.js` copy hook, `src/_includes/layout.njk` L46, `src/oal-version.njk`, `src/_headers` | `DEPLOY.md` cost 4, and the owner's decision to close it rather than measure it again | The frozen stylesheet is served **content-addressed** — `/oal/v1.0/styles.<sha>.css` — and stored bare as `styles.css`. `_headers` moves from the exact path to `/oal/v1.0/styles.*`. | **`immutable` at a stable URL is only correct while a published version never changes, and re-freezes exist.** A visitor holding the old sheet renders the new document against it for up to a year and no purge reaches them; 2026-08-13 was safe by luck, not construction. Now the URL moves when the bytes move and the stale copy is never requested again, because the document is `max-age=0` and always names the current name. **It is free only while v1.0 is pre-effective** — changing a published version's asset URLs afterwards is itself a version event — which is the italic re-subset's argument of 2026-08-09 applied a second time. Two things the first attempt got wrong, both caught rather than reasoned about: the sheet was put in a `css/` subdirectory, which re-based all four relative `@font-face` URLs to `/oal/v1.0/css/fonts/…` and **check 17 reported four font files fetched and none of them under the build**; and `_headers` then wanted a splat this file already rejects. Both are answered by keeping the sheet in the version's own directory and using `styles.*` — a splat in the TRAILING position with a literal prefix, which is the documented form, and which cannot overlap `fonts/*` or `favicon.svg`. |
| 135 | `tests/checks/34-frozen-assets-addressed.test.js` — new; `tests/checks/21`, `27`, `32` — name resolution | row 134 | Check 34 asserts every immutable URL under a frozen version names its own bytes, with fonts and favicon as a **named literal residual** rather than a glob. Checks 21, 27 and 32 resolve the served name through `builtStylesheet` instead of spelling `styles.css`. | **Only one of the three older guards noticed the rename, and the two that did not are the interesting ones.** Check 27's chrome-order regex pinned `styles.css` and went red — correct behaviour, and it is widened rather than loosened to `[^"]*\.css`, which would also match the chrome sheet and let the order assertion pass against itself. Check 21's asset arm resolves the built path and then skips it when `existsSync` comes back false, so an unfindable name is a **skip** rather than a failure: the byte-identity assertion proving the build serves the snapshot rather than `src/` silently stopped covering the stylesheet. Check 32's half-stored control deletes the built stylesheet to prove a partial snapshot is refused — `rm --force` on an absent path is silent, so it was asserting over an intact snapshot and the control could not fail. Both are the undefended-population pathology, found by drilling the fingerprint out rather than by reading. Check 34's own first draft had it too: its second arm compared the served name against `stylesheetFile(bytes)` and stayed green under the drill because both sides moved together; it now asserts `isContentAddressed` first, which knows nothing about the naming helper. The residual is a literal list because `fonts/*` would also excuse a stylesheet someone moved into `fonts/`. Baselines re-measured on all four targets: build **133/123/0/10**, live-local **133/131/0/2**, handover **133/80/10/43** with failures **held at 10**, empty **133/56/67/10**. `EMPTY_EXPECTED_FAILURES` 65 → 67; `HANDOVER_EXPECTED_FAILURES` unchanged. |
| 136 | `src/_data/oal.json` — seven level descriptors and evidence rows across dimensions 2, 3, 4, 5, 6 and 7 | the determination catalogue of 2026-08-14, and the owner's decision to amend v1.0 before its effective date | Seven criteria tightened so each supports a yes-or-no determination. Quoted in full in *The seven tightenings* below. 14 strings, 14 lines, no other byte of the file moved. | **A version event, taken as a pre-effective amendment rather than as v1.1 or v2.0.** The catalogue found the published wording would not support a binary determination in these places, and deferred them — which left the instrument knowingly wrong on one criterion: dimension 3's deny case would award OAL 3 on a global row count that an in-place update walks straight past, and the methodology says so in terms. **The catalogue also contradicted itself**: rows D3-L3-04, D5-L2-03 and D7-L2-05 already stated the tightened rule as operative while the section governing them said the assessor scores the published wording, so two assessors reading it faithfully reached different answers. **Seven, not the six the catalogue listed** — D6-L2-00 carried a "See §H" reference and appeared in no table; it is the same shape as tightenings 1 and 3, a denominator for a completeness criterion, and adopting the six while deferring it would have recreated the contradiction being closed. The clarifying/breaking labels are deliberately **not** relied on: on audit at least four of the seven fail §H's own symmetric test, which asks whether a score could be *different*, not whether it could be lower. Rubric §H predicted precisely this — *"the first breaking change will be tempting to classify as clarifying, because the classification is self-applied and the incentive runs one way"* — so the route chosen does not depend on the classification at all. Not changed: every `selfCheck`, `inspectedCap`, `inspectedMax`, `testedMax`, `buyerQuestion`, the depth grid, and every level 0/1/3 descriptor not listed below. **No depth cap moves.** |
| 137 | `src/_data/copy/oal.md` `changes.body`, `src/_data/copy/changelog.md` `intro` | rubric §H offering no route for what this change is | A third case added to the published change policy: **before a version takes effect**, its text may be amended in place rather than superseded, because no score exists that an amendment could move. The window closes on the effective date or the first scorecard, whichever comes first. | **The instrument's own change policy did not cover the change being made to it, and pretending otherwise was the alternative.** §H offered exactly two routes — v1.1 for clarifying, v2.0 for breaking — and in-place amendment is neither. This amendment is authorised by `DEPLOY.md`'s re-freeze practice and the owner's decision under it, not by the rubric; the clause is added so the **next** one is governed rather than a third exception. That ordering matters and is not circular: the clause does not retroactively license this change, it describes the window this change happened to be in and closes it behind itself. It is inside `<main>`, so it is part of the same version event and the same re-freeze. Mirrored on `/changelog/`, which restates the classification policy for a reader who never opens the rubric. |
| 138 | `versions/v1.0.json`, `versions/v1.0/`, `versions/v1.0.published-index.html`, `tools/freeze-version.mjs` `PUBLISHED_SHA256` | rows 136 and 137 | **v1.0 re-frozen, the third time.** Anchor `da0ed36ecf24…` → `9573e343fbb2…`. `main.html` 42,420 → 44,748 bytes. | **The version event, executed by the documented override**: delete the manifest, the pinned directory AND the retained document, rebuild, re-freeze, then move the literal in a reviewed diff. Check 21's sixth test went red on the rubric edit and nothing else did — *"a current version says the same thing at both of its addresses"* — exactly as row 114 recorded for the previous one, and it is the only signal a version event produces. §3.4.6's trap was drilled rather than assumed: the build serves the fragment from stored bytes, so a regeneration can silently re-record the OLD text and report success. Measured after deleting all three, all eight new passages are present in the regenerated fragment. What moved: `main.html`, deliberately; `styles.css` **935d5f33 → ef0b25e2**, because a re-freeze copies the current `src/styles.css` and that sheet carries `47d216b`'s grid repair; fonts and favicon byte-identical. R2's hazard was measured **before** the re-freeze this time, by `tools/frozen-render-diff.mjs`: **0 computed-style differences across 103,872 values**, 541 elements inside the frozen `<main>`, at four widths. And cost 4 is closed for this very re-freeze rather than for the next one — the served URL moved with the bytes, `/oal/v1.0/styles.935d5f33.css` → `styles.ef0b25e2.css`, so nobody holding the old sheet is handed the new document to render against it. Page weight: `/oal/` **150,985 → 152,025 B against 153,600**, headroom 2,615 → **1,575 B**. It fits, and it is the tightest page on the site, so the next paragraph added to the rubric needs measuring before it is written. `npm run check` **133/123/0/10**. |

| 139 | `tests/checks/15-live-deploy.test.js` frozen-asset arm, `tests/checks/28-html-revalidates.test.js` frozen-asset list | the `/ship` pre-landing review, tracing the new served filename through every consumer | Both resolve the served stylesheet through `builtStylesheet` instead of spelling `styles.css`; check 15 gains `assert.equal(status, 200)`; check 28's silent `continue` on an absent frozen asset becomes a finding. | **Fingerprinting the frozen stylesheet blinded both of the guards that prove it is cached correctly, and neither of them went red.** Check 15 asserts the frozen asset is served `immutable` at the edge. `live()` returns `ok: true` for any HTTP response — it only reports transport failures — and `_headers`' `/oal/v1.0/styles.*` rule matches a URL whether or not a file is behind it. Measured: `/oal/v1.0/styles.css` → **`status=404`, `cache-control: public, max-age=31536000, immutable`**. So the one check that proves immutability *at the edge* was asserting it about a file that no longer exists, and would have gone on passing against production Cloudflare indefinitely. Check 28 lost the same asset by a different route: a hardcoded `'styles.css'` in its list, `existsSync` false, `continue` — population 3 → 2, the check green, and the single asset the arm exists to defend no longer covered. **Both are the undefended-population pathology, and this time the guards were blinded by my own change rather than written blind.** Found by the Enum & Value Completeness pass — tracing one new value through all fourteen of its consumers — which is the only discipline in the checklist that would have caught it; every test suite was green throughout. Drilled: check 28 red with the sheet absent and green with it restored; check 15 red when pointed at the 404 URL, naming the status, and green when resolved. Four targets unmoved: build **133/123/0/10**, live-local **133/131/0/2**, handover **133/80/10/43**, empty **133/56/67/10**. |

## Flagged and left — nothing was changed

| # | Where | Source | What | Why it was left |
|---|---|---|---|---|
| A | About vs Independence | handover `about.html` L46, `independence.html` L39 | The same sentence appears on both pages, with *"nobody asks whether their tracing vendor is **third-party**"* on About and *"…is **independent**"* on Independence | Word choice in settled copy. §8 is explicit: tone, rhythm and word choice are flagged and left. Both readings are defensible — the Independence page is discussing the word *independent*, which is arguably why it uses it there — and resolving it is a copy decision, not an engineering one. **Closed 2026-08-11 by row 61** — draft 4's rule settled the copy decision (*third-party*, until the independence apparatus exists), and both sentences now read from `{partyWord}`, so the phase-2 flip moves them together. |
| B | Home, failure block | `RATIONALE.md` "Two items I did not change" | *"In a system Ordoia built…"* sits two paragraphs above a constraints link saying Ordoia does not assess systems it has built | Already flagged by the design pass. Settled copy, and the passage is method evidence rather than a client case study, so it clears §2 as written. A reader who notices both will still ask. |
| C | Services, How we work | `RATIONALE.md` "Two items I did not change" | *"led end-to-end by a named senior architect"*, singular | Already flagged by the design pass. It is the one place the copy itself is sized to one of something, while the roster instruction elsewhere is structural. **Closed 2026-08-11 by row 60** — it now reads *"the senior architect named on the scorecard"*, which claims no headcount in either direction and states something the scorecard already makes true. |
| D | Footer | `BRIEF.md` §10, §12.1 | Terms and Privacy are absent rather than present-and-disabled | §10 forbids stubbing them and forbids a link resolving to nothing; §12.1 makes the entity a flag-and-stop. The handover's footer names neither, which satisfies both rules today. Adding a line about them would be new copy on a stopped decision. |
| E | Every page carrying the version stamp | `src/_data/site.json` | The site goes live in August 2026 printing *"Version 1.0, published 2026-09-19"* — a publication date in its own future | **Deliberate, and recorded here rather than left for a reader to find.** The date was decided on 2026-08-08 as a practice decision (row 16) and is a permanent address on every scorecard issued under OAL v1.0; re-dating it to the day the site happened to go live would reopen a settled decision for an engineering convenience, and it can only be reopened before the first scorecard exists. The alternative — holding the custom domain until 19 September — was considered and declined: the infrastructure is the same either way, and the site is more useful readable. **No check in this repository can detect this**, which is the reason it is a row: check 15 proves the bytes survive the edge, not that they are true. What the date will mean is the freeze — `/oal/v1.0/` is caught in `versions/v1.0.json` at first publication and cannot change afterwards, so "published" is a claim about immutability, not about first availability. Read it as an effective date, in the sense a standards body uses one. |
| F | Home, the basis block | draft 5 §6.5, STOP-POINT 1 | The block ships with **three** bullets. There is no bullet stating who bears travel or expenses | **Escalated rather than decided, and the decision was to leave it.** §6.5's source ruling says *"ship four bullets, never a placeholder"*, on the ground that leaving expenses out answers the question in the client's favour, in writing, on a public page. That reasoning is accepted and the consequence is recorded here rather than absorbed: **silence on this page is an answer, and it is the client-favourable one.** Three wordings were put to the practice and none was chosen, so what ships is the silence. The fourth bullet was also contested — §6.5 lists three real bullets plus a placeholder for the *reliance* bullet, which is commit C, and expenses and reliance cannot both be the fourth. Reliance is now unblocked (the engagement-letter template exists) but deferred by decision, so it will arrive as the fourth. Reopen either when the engagement letter is being read for its disclosure clause. |
| G | Home and Services, the coverage x depth grid, at 320px and 375px | draft 5 §5, measured on the rendered page | The **Retainer** cell — product name and price both — sits entirely outside the scroller's visible box at rest. The last product on the ladder is invisible to anyone who does not discover the horizontal scroll | **Measured, pre-existing, and not fixable inside this session's remit.** §5 predicted this shape of defect exactly — *"a price is reachable only by scrolling, which is worse than overflow, because it looks fine"* — and its prescribed fix does not reach it. §5 assumed the cell was one pre-joined string and asked for it to be split into fields so the meta could take its own line; the cell was already two stacked blocks, so name and price are always together **with each other** and the problem is that the whole column is off-screen. Numbers at 320px: scroller 272px, table **418px**, overflow **146px**; columns Coverage 101, Inspected 91, Tested 90, **Sustained 136**, the last driven by `from £3,000/month + VAT` at 120px. Row 63's field composition cut the cell's height from 82px to 61px and left the width where it was. **This is not new and was not caused by the VAT suffix**: measured at commit `c0e9b6e`, which is byte-identical to what launched, the overflow was **82px** and the retainer cell was already off-screen. The suffix widened it, it did not create it — the defect has been live since launch and no check could see it. It is left because the only fixes that work are design decisions rather than engineering ones: a four-column grid does not fit 320px, which is *why* row 14 introduced the scroller in the first place, and trimming every column to its floor still lands near 325px against 272px available. The real options are to let the grid reflow to a stacked layout below 46rem — reversing row 14's reading of WCAG 1.4.10 — or to add a visible scroll affordance so the fourth column announces itself. Both change the site's central commercial instrument and belong to whoever owns the design, not to this session. **Closed 2026-08-13 by rows 97–100**, on the first of those two options: draft 6 §8 ruled the reflow, which is the design decision this row said was missing. Re-measured before the fix at 320px — "Retainer" at 339..402px against a 320px viewport, its price at 339..492px — so the row's account was exact, three sessions later, and the numbers had not drifted. |

| H | Home and About, the email CTA line | draft 6 §5.5 | The line *"Or email hello@ordoia.com."* **ships nowhere**, and the batch is seven edits and a footer rather than eight. | §5.5 branches on whether each page carries a **secondary** CTA, and the answer is measured: Home renders exactly one CTA — the audit card at `index.njk:22` — and **About renders none at all**. §5.5's third branch is therefore the live one, and it says so explicitly: the line is not attached to a lone primary instead, because the whole point of a primary is that it is the one action and a fallback route beneath it manufactures a second. The footer address now reaches every page, including `/oal/v1.0/`, and carries it. The durable placement rule §9.7 floats — *beneath the page's last action, as body text* — was **not** adopted to resolve this; the branch was taken as written. Reported rather than quietly absorbed, because the batch size is something the practice should know. |

## Still not ours to decide

From `BRIEF.md` §12 and `RATIONALE.md`. One item remains:

1. **The entity.** Terms and Privacy stay unbuilt, and `_redirects` deliberately routes
   neither — a redirect to the home page would be a link resolving to the wrong thing,
   which is worse than one resolving to nothing.

### Closed since

- **The publication date** — decided 2026-08-08 as **`2026-09-19`**. See row 16.
- **The two failure-mode bullets** — the §12.3 stop was lifted and they are written.
  See row 17.
- **The domain** — decided 2026-08-08 as **`ordoia.com`**. See rows 20 and 21.
- **The commit-or-deploy reference on the scorecard face** — it **stays**, and the
  reproducibility sentence on About stands as written. A commit hash printed on a
  scorecard against a repository nobody can read is a provenance claim the reader cannot
  check, which is the OAL 1 failure this practice sells against — a behaviour asserted
  with nothing verifying it. Against a readable one it is evidence.

  > **Correction, 2026-08-09.** This entry previously read *"contingent on something that
  > has now been settled: the repository is public"*. **That was false when it was
  > written and is still false: there is no git remote at all.** See row 27. The decision
  > stands; its premise does not yet. Publishing the repository is the act that makes this
  > entry true, and until it happens this bullet is recorded here rather than under a
  > heading claiming it is closed.
  >
  > **Closed 2026-08-09.** The repository is public, at `github.com/udtrad/ordoia`, and
  > `main` is `ec26096`. The premise is now true and the claim can be read the way it was
  > always meant to be: a commit reference on the face of a scorecard resolves to bytes a
  > reader can fetch and hash for themselves. **The correction above stays.** A provenance
  > document that deletes the record of having been wrong is worth less than one that
  > carries it, and the span between the two dates — one day — is itself the evidence that
  > the flag-and-fix rule works.

## The seven tightenings

The published wording of each criterion, and what it says now. Recorded here in full
because `src/_data/oal.json`'s own header requires it — *"Every string here is the settled
copy, transcribed verbatim from the designer handover. Departures are logged in CHANGES.md
— none are silent"* — and because check 12 will not let rendered prose exist that traces to
neither a copy file nor this document. Row 136 is the reasoning; this is the diff.

### 1 · dimension 2, level 2 — the tool inventory's enumeration method

**Level descriptor.**

> **Was.** Tool outcomes are typed and checked in code. No user-visible confirmation is issued unless the call returned a typed success. Error paths produce a coded response rather than a narrated one. Every tool has a defined failure behaviour, written down, and the list of tools is complete.

> **Is.** Tool outcomes are typed and checked in code. No user-visible confirmation is issued unless the call returned a typed success. Error paths produce a coded response rather than a narrated one. Every tool has a defined failure behaviour, written down, and the list of tools is complete — against the statically registered tools together with a live listing of every dynamically discovered toolset. Where a toolset is mounted without a filter, the inventory is not enumerable from code and the list is not complete until that listing is obtained.

**Evidence required.**

> **Was.** The result type and the check on it; the tool inventory with per-tool failure behaviour; one observed failure path end to end.

> **Is.** The result type and the check on it; the tool inventory with per-tool failure behaviour, and the live listing behind it wherever a toolset is mounted without a filter; one observed failure path end to end.

### 2 · dimension 3, level 2 — the egress map's denominator

**Level descriptor.**

> **Was.** Access filtering is applied at query time inside the index or store, derived from a server-owned identity claim: a validated token claim or a session-bound value the model cannot influence. Egress is mapped: what leaves the boundary, to which vendor, and what is written to logs and traces.

> **Is.** Access filtering is applied at query time inside the index or store, derived from a server-owned identity claim: a validated token claim or a session-bound value the model cannot influence. Egress is mapped: what leaves the boundary, to which vendor, and what is written to logs, traces and metrics. The map is complete when every outbound destination reachable from the path appears in it, checked against an enumeration derived from the code rather than from recollection.

**Evidence required.**

> **Was.** The query-time filter and the claim it derives from, traced back to token validation; the egress map naming vendors and log destinations.

> **Is.** The query-time filter and the claim it derives from, traced back to token validation; the egress map naming vendors and log, trace and metric destinations, with the code-derived enumeration it was checked against.

### 3 · dimension 4, level 2 — the privileged-action set

**Level descriptor.**

> **Was.** Refusal conditions are determined outside the model wherever they are determinable — schema, allowlist, deterministic classifier, tool-level gate. Content retrieved from documents or returned by tools is structurally marked as data, and any privileged action requires a condition that retrieved content cannot satisfy.

> **Is.** Refusal conditions are determined outside the model wherever they are determinable — schema, allowlist, deterministic classifier, tool-level gate. Content retrieved from documents or returned by tools is structurally marked as data, and any privileged action — the tools that write, spend, or move data outside the authorisation boundary, enumerated from the tool inventory — requires a condition that retrieved content cannot satisfy.

**Evidence required.**

> **Was.** The code gate, schema or allowlist; the assembly path showing the data/instruction separation; the privileged-action condition and what supplies it.

> **Is.** The code gate, schema or allowlist; the assembly path showing the data/instruction separation; the privileged-action inventory, and per action the condition and what supplies it.

### 4 · dimension 6, level 2 — the model-call inventory

**Level descriptor.**

> **Was.** Versions are pinned exactly — primary, fallback and embedding models — and an upgrade is a deliberate, reviewable change in version control. Where the embedding model is pinned, the re-embedding consequence of changing it is documented.

> **Is.** Versions are pinned exactly at every site that supplies or defaults a model identifier — primary, fallback, embedding, re-ranking and scorer models among them — and an upgrade is a deliberate, reviewable change in version control. Where the embedding model is pinned, the re-embedding consequence of changing it is documented.

**Evidence required.**

> **Was.** Exact versions across every model call including fallbacks and embeddings, in version control, with change history.

> **Is.** Exact versions across every model call — including fallbacks, embeddings, re-rankers and scorers, and including environment defaults, deployment manifests and test fixtures — in version control, with change history.

### 5 · dimension 3, level 3 — the scope of the no-mutation assertion

**Level descriptor.**

> **Was.** Deny-case scoring in the eval suite: cases asserting refusal for out-of-scope identities, run on every build, sourced from the real role definitions rather than invented. For any tool that writes, the deny case asserts no mutation occurred, not merely that the answer was a refusal.

> **Is.** Deny-case scoring in the eval suite: cases asserting refusal for out-of-scope identities, run on every build, sourced from the real role definitions rather than invented. For any tool that writes, the deny case asserts that no mutation occurred — not merely that the answer was a refusal — by a check scoped to the records the case could have touched and able to detect an update in place. A count over a whole table does not establish this, because an in-place update leaves the count unchanged.

**Evidence required.**

> **Was.** The deny-case suite; its identity source mapped to the real role matrix; CI wiring; per-case assertions including the no-mutation check; run history.

> **Is.** The deny-case suite; its identity source mapped to the real role matrix; CI wiring; per-case assertions including the no-mutation check and the records it is scoped to; run history.

### 6 · dimension 5, level 2 — the per-case record of scorer type

**Level descriptor.**

> **Was.** A fixed set under version control; each case run repeatedly and scored on the distribution rather than the sample; deterministic scorers wherever the property is deterministically checkable; a stated pass rule — for example, k of n runs — rather than an implied one.

> **Is.** A fixed set under version control; each case run repeatedly and scored on the distribution rather than the sample; each case's scorer type recorded, and deterministic scorers wherever the property is deterministically checkable — a model-graded scorer on a deterministically checkable property is a finding rather than a choice; a stated pass rule — for example, k of n runs — rather than an implied one.

**Evidence required.**

> **Was.** Harness code showing more than one run per case; the scorer implementations; the written pass rule; version-control history of the set.

> **Is.** Harness code showing more than one run per case; the scorer implementations and the per-case record of which type scored which case; the written pass rule; version-control history of the set.

### 7 · dimension 7, level 2 — tracing in place at run time

**Level descriptor.**

> **Was.** End-to-end tracing of the agent path — prompt, retrieval, tool calls, output — with sensitive content scrubbed. Alerting on quality signals such as refusal rate, empty-retrieval rate and tool-error rate, not only uptime and latency.

> **Is.** End-to-end tracing of the agent path — prompt, retrieval, tool calls, output — with sensitive content scrubbed, and in place at run time: a pipeline that can stop silently has not been shown to be tracing, so its absence must raise. Alerting on quality signals such as refusal rate, empty-retrieval rate and tool-error rate, not only uptime and latency.

**Evidence required.**

> **Was.** Trace exemplars covering a complete turn; the scrubbing configuration; alert rules defined on quality signals with thresholds.

> **Is.** Trace exemplars covering a complete turn; the scrubbing configuration; the exporter's failure mode and what raises when it stops; alert rules defined on quality signals with thresholds.

